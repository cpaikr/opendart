openapi: 3.2.0
info:
  title: Invalid lint compatibility fixture
  version: 1.0.0
paths:
  /broken:
    get:
      operationId: get_broken_fixture
      responses:
        "200":
          content:
            application/json:
              schema:
                type: object
