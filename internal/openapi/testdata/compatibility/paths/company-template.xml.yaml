parameters:
  - name: corp_code
    in: path
    required: true
    schema:
      type: string
put:
  operationId: put_company_xml_fixture
  responses:
    "200":
      description: Representative templated PUT XML response
      content:
        application/xml:
          schema:
            $ref: ../schemas/company.yaml
patch:
  operationId: patch_company_xml_fixture
  responses:
    "200":
      description: Representative templated PATCH XML response
      content:
        application/xml:
          schema:
            $ref: ../schemas/company.yaml
