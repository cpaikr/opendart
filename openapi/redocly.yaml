extends:
  - recommended-strict

rules:
  # These fields are not part of the official endpoint guide.
  info-license: off
  info-license-strict: off
  operation-4xx-response: off

  # Remote or ambiguous references would make the archival spec non-portable.
  spec-strict-refs: error
