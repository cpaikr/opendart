// @generated by opendart-tool; DO NOT EDIT.
// cli-dispatch-generator-schema: 1
// cli-dispatch-projection-sha256: 263e5fd73af9d4bf17220d610463eb1d7fe548c99de1d624ac74580e4c5d0d02

use clap::ArgMatches;
use opendart::PrepareError;

use crate::interface_projection::catalog::operation;
use crate::prepared::{self, PreparedCall};

pub(crate) fn prepare_call(matches: &ArgMatches) -> Result<PreparedCall, PrepareError> {
    match matches.subcommand() {
        Some(("accounting-auditor-name-and-audit-opinion" | "DS002-2020009", matches)) => {
            let input = opendart::operations::AccnutAdtorNmNdAdtOpinion::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("accounting-auditor-name-and-audit-opinion").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::AccnutAdtorNmNdAdtOpinionJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::AccnutAdtorNmNdAdtOpinionXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("audit-service-contracts" | "DS002-2020010", matches)) => {
            let input = opendart::operations::AdtServcCnclsSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("audit-service-contracts").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::AdtServcCnclsSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::AdtServcCnclsSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("bond-with-warrants-issuance-decisions" | "DS005-2020034", matches)) => {
            let input = opendart::operations::BdwtIsDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("bond-with-warrants-issuance-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::BdwtIsDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::BdwtIsDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("bonus-share-issue-decisions" | "DS005-2020024", matches)) => {
            let input = opendart::operations::FricDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("bonus-share-issue-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::FricDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::FricDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("business-acquisition-decisions" | "DS005-2020042", matches)) => {
            let input = opendart::operations::BsnInhDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("business-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::BsnInhDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::BsnInhDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("business-suspensions" | "DS005-2020020", matches)) => {
            let input = opendart::operations::BsnSp::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("business-suspensions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::BsnSpJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::BsnSpXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("business-transfer-decisions" | "DS005-2020043", matches)) => {
            let input = opendart::operations::BsnTrfDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("business-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::BsnTrfDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::BsnTrfDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("capital-increase-and-reduction-status" | "DS002-2019004", matches)) => {
            let input = opendart::operations::IrdsSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("capital-increase-and-reduction-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::IrdsSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::IrdsSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("capital-reduction-decisions" | "DS005-2020026", matches)) => {
            let input = opendart::operations::CrDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("capital-reduction-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::CrDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::CrDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("combined-paid-in-and-bonus-share-issue-decisions" | "DS005-2020025", matches)) => {
            let input = opendart::operations::PifricDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("combined-paid-in-and-bonus-share-issue-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::PifricDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::PifricDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("commercial-paper-outstanding-balance" | "DS002-2020004", matches)) => {
            let input = opendart::operations::EntrprsBilScritsNrdmpBlce::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("commercial-paper-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::EntrprsBilScritsNrdmpBlceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::EntrprsBilScritsNrdmpBlceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("companies-financial-indicators" | "DS003-2022002", matches)) => {
            let input = opendart::operations::FnlttCmpnyIndx::new(matches.get_many::<String>("corp_code").expect("clap requires input").cloned().collect::<Vec<_>>(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone(), matches.get_one::<String>("idx_cl_code").expect("clap requires input").clone());
            let operation = operation("companies-financial-indicators").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::FnlttCmpnyIndxJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::FnlttCmpnyIndxXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("companies-key-accounts" | "DS003-2019017", matches)) => {
            let input = opendart::operations::FnlttMultiAcnt::new(matches.get_many::<String>("corp_code").expect("clap requires input").cloned().collect::<Vec<_>>(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("companies-key-accounts").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::FnlttMultiAcntJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::FnlttMultiAcntXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-financial-indicators" | "DS003-2022001", matches)) => {
            let input = opendart::operations::FnlttSinglIndx::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone(), matches.get_one::<String>("idx_cl_code").expect("clap requires input").clone());
            let operation = operation("company-financial-indicators").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::FnlttSinglIndxJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::FnlttSinglIndxXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-financial-statements" | "DS003-2019020", matches)) => {
            let input = opendart::operations::FnlttSinglAcntAll::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone(), matches.get_one::<String>("fs_div").expect("clap requires input").clone());
            let operation = operation("company-financial-statements").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::FnlttSinglAcntAllJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::FnlttSinglAcntAllXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-key-accounts" | "DS003-2019016", matches)) => {
            let input = opendart::operations::FnlttSinglAcnt::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("company-key-accounts").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::FnlttSinglAcntJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::FnlttSinglAcntXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-merger-decisions" | "DS005-2020050", matches)) => {
            let input = opendart::operations::CmpMgDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("company-merger-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::CmpMgDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::CmpMgDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-overview" | "DS001-2019002", matches)) => {
            let input = opendart::operations::Company::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone());
            let operation = operation("company-overview").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::CompanyJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::CompanyXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-split-decisions" | "DS005-2020051", matches)) => {
            let input = opendart::operations::CmpDvDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("company-split-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::CmpDvDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::CmpDvDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-split-merger-decisions" | "DS005-2020052", matches)) => {
            let input = opendart::operations::CmpDvmgDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("company-split-merger-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::CmpDvmgDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::CmpDvmgDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("contingent-capital-securities-outstanding-balance" | "DS002-2020008", matches)) => {
            let input = opendart::operations::CndlCaplScritsNrdmpBlce::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("contingent-capital-securities-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::CndlCaplScritsNrdmpBlceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::CndlCaplScritsNrdmpBlceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("convertible-bond-issuance-decisions" | "DS005-2020033", matches)) => {
            let input = opendart::operations::CvbdIsDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("convertible-bond-issuance-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::CvbdIsDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::CvbdIsDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("corporate-bond-outstanding-balance" | "DS002-2020006", matches)) => {
            let input = opendart::operations::CprndNrdmpBlce::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("corporate-bond-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::CprndNrdmpBlceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::CprndNrdmpBlceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("creditor-bank-management-commencements" | "DS005-2020027", matches)) => {
            let input = opendart::operations::BnkMngtPcbg::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("creditor-bank-management-commencements").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::BnkMngtPcbgJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::BnkMngtPcbgXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("creditor-bank-management-discontinuations" | "DS005-2020036", matches)) => {
            let input = opendart::operations::BnkMngtPcsp::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("creditor-bank-management-discontinuations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::BnkMngtPcspJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::BnkMngtPcspXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("debt-securities-issuance-results" | "DS002-2020003", matches)) => {
            let input = opendart::operations::DetScritsIsuAcmslt::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("debt-securities-issuance-results").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::DetScritsIsuAcmsltJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::DetScritsIsuAcmsltXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("debt-securities-registrations" | "DS006-2020055", matches)) => {
            let input = opendart::operations::BdRs::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("debt-securities-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::BdRsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::BdRsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("default-events" | "DS005-2020019", matches)) => {
            let input = opendart::operations::DfOcr::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("default-events").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::DfOcrJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::DfOcrXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("demerger-registrations" | "DS006-2020059", matches)) => {
            let input = opendart::operations::DvRs::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("demerger-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::DvRsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::DvRsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("depositary-receipt-registrations" | "DS006-2020056", matches)) => {
            let input = opendart::operations::StkdpRs::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("depositary-receipt-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::StkdpRsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::StkdpRsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("director-auditor-compensation-by-type" | "DS002-2020015", matches)) => {
            let input = opendart::operations::DrctrAdtAllMendngSttusMendngPymntamtTyCl::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("director-auditor-compensation-by-type").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::DrctrAdtAllMendngSttusMendngPymntamtTyClJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::DrctrAdtAllMendngSttusMendngPymntamtTyClXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("director-auditor-individual-compensation" | "DS002-2019012", matches)) => {
            let input = opendart::operations::HmvAuditIndvdlBySttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("director-auditor-individual-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::HmvAuditIndvdlBySttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::HmvAuditIndvdlBySttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("director-auditor-individual-compensation-v2" | "DS002-2026001", matches)) => {
            let input = opendart::operations::HmvAuditIndvdlBySttusV2::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("director-auditor-individual-compensation-v2").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::HmvAuditIndvdlBySttusV2JsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::HmvAuditIndvdlBySttusV2XmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("director-auditor-total-compensation" | "DS002-2019013", matches)) => {
            let input = opendart::operations::HmvAuditAllSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("director-auditor-total-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::HmvAuditAllSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::HmvAuditAllSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("dissolution-events" | "DS005-2020022", matches)) => {
            let input = opendart::operations::DsRsOcr::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("dissolution-events").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::DsRsOcrJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::DsRsOcrXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("dividend-information" | "DS002-2019005", matches)) => {
            let input = opendart::operations::AlotMatter::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("dividend-information").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::AlotMatterJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::AlotMatterXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("download-company-codes" | "DS001-2019018", _matches)) => {
            let input = opendart::operations::CorpCode::new();
            let operation = operation("download-company-codes").expect("generated catalog entry");
            { let request: opendart::PreparedBinaryRequest = input.prepare_zip()?; let _response_type: Option<opendart::BinaryReply<opendart::BodyStream>> = None; Ok(prepared::binary(operation, request)) }
        }
        Some(("download-disclosure-document" | "DS001-2019003", matches)) => {
            let input = opendart::operations::Document::new(matches.get_one::<String>("rcept_no").expect("clap requires input").clone());
            let operation = operation("download-disclosure-document").expect("generated catalog entry");
            { let request: opendart::PreparedBinaryRequest = input.prepare_zip()?; let _response_type: Option<opendart::BinaryReply<opendart::BodyStream>> = None; Ok(prepared::binary(operation, request)) }
        }
        Some(("download-xbrl-financial-statements" | "DS003-2019019", matches)) => {
            let input = opendart::operations::FnlttXbrl::new(matches.get_one::<String>("rcept_no").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("download-xbrl-financial-statements").expect("generated catalog entry");
            { let request: opendart::PreparedBinaryRequest = input.prepare_zip()?; let _response_type: Option<opendart::BinaryReply<opendart::BodyStream>> = None; Ok(prepared::binary(operation, request)) }
        }
        Some(("employee-status" | "DS002-2019011", matches)) => {
            let input = opendart::operations::EmpSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("employee-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::EmpSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::EmpSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("equity-securities-registrations" | "DS006-2020054", matches)) => {
            let input = opendart::operations::EstkRs::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("equity-securities-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::EstkRsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::EstkRsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("exchangeable-bond-issuance-decisions" | "DS005-2020035", matches)) => {
            let input = opendart::operations::ExbdIsDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("exchangeable-bond-issuance-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::ExbdIsDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::ExbdIsDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("executive-status" | "DS002-2019010", matches)) => {
            let input = opendart::operations::ExctvSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("executive-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::ExctvSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::ExctvSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("hybrid-capital-securities-outstanding-balance" | "DS002-2020007", matches)) => {
            let input = opendart::operations::NewCaplScritsNrdmpBlce::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("hybrid-capital-securities-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::NewCaplScritsNrdmpBlceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::NewCaplScritsNrdmpBlceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("insider-shareholdings" | "DS004-2019022", matches)) => {
            let input = opendart::operations::Elestock::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone());
            let operation = operation("insider-shareholdings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::ElestockJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::ElestockXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("largest-shareholder-changes" | "DS002-2019008", matches)) => {
            let input = opendart::operations::HyslrChgSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("largest-shareholder-changes").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::HyslrChgSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::HyslrChgSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("largest-shareholder-status" | "DS002-2019007", matches)) => {
            let input = opendart::operations::HyslrSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("largest-shareholder-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::HyslrSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::HyslrSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("lawsuit-filings" | "DS005-2020028", matches)) => {
            let input = opendart::operations::LwstLg::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("lawsuit-filings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::LwstLgJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::LwstLgXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("major-shareholdings" | "DS004-2019021", matches)) => {
            let input = opendart::operations::Majorstock::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone());
            let operation = operation("major-shareholdings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::MajorstockJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::MajorstockXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("merger-registrations" | "DS006-2020057", matches)) => {
            let input = opendart::operations::MgRs::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("merger-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::MgRsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::MgRsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("minority-shareholder-status" | "DS002-2019009", matches)) => {
            let input = opendart::operations::MrhlSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("minority-shareholder-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::MrhlSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::MrhlSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("non-audit-service-contracts" | "DS002-2020011", matches)) => {
            let input = opendart::operations::AccnutAdtorNonAdtServcCnclsSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("non-audit-service-contracts").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::AccnutAdtorNonAdtServcCnclsSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::AccnutAdtorNonAdtServcCnclsSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("other-asset-transactions-and-putback-options" | "DS005-2020018", matches)) => {
            let input = opendart::operations::AstInhtrfEtcPtbkOpt::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("other-asset-transactions-and-putback-options").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::AstInhtrfEtcPtbkOptJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::AstInhtrfEtcPtbkOptXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("other-company-equity-securities-acquisition-decisions" | "DS005-2020046", matches)) => {
            let input = opendart::operations::OtcprStkInvscrInhDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("other-company-equity-securities-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::OtcprStkInvscrInhDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::OtcprStkInvscrInhDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("other-company-equity-securities-transfer-decisions" | "DS005-2020047", matches)) => {
            let input = opendart::operations::OtcprStkInvscrTrfDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("other-company-equity-securities-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::OtcprStkInvscrTrfDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::OtcprStkInvscrTrfDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("other-corporation-investment-status" | "DS002-2019015", matches)) => {
            let input = opendart::operations::OtrCprInvstmntSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("other-corporation-investment-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::OtrCprInvstmntSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::OtrCprInvstmntSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("outside-director-status-and-changes" | "DS002-2020012", matches)) => {
            let input = opendart::operations::OutcmpnyDrctrNdChangeSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("outside-director-status-and-changes").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::OutcmpnyDrctrNdChangeSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::OutcmpnyDrctrNdChangeSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("overseas-market-delisting-decisions" | "DS005-2020030", matches)) => {
            let input = opendart::operations::OvDlstDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("overseas-market-delisting-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::OvDlstDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::OvDlstDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("overseas-market-delistings" | "DS005-2020032", matches)) => {
            let input = opendart::operations::OvDlst::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("overseas-market-delistings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::OvDlstJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::OvDlstXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("overseas-market-listing-decisions" | "DS005-2020029", matches)) => {
            let input = opendart::operations::OvLstDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("overseas-market-listing-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::OvLstDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::OvLstDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("overseas-market-listings" | "DS005-2020031", matches)) => {
            let input = opendart::operations::OvLst::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("overseas-market-listings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::OvLstJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::OvLstXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("paid-in-capital-increase-decisions" | "DS005-2020023", matches)) => {
            let input = opendart::operations::PiicDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("paid-in-capital-increase-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::PiicDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::PiicDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("private-placement-proceeds-usage" | "DS002-2020017", matches)) => {
            let input = opendart::operations::PrvsrpCptalUseDtls::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("private-placement-proceeds-usage").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::PrvsrpCptalUseDtlsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::PrvsrpCptalUseDtlsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("public-offering-proceeds-usage" | "DS002-2020016", matches)) => {
            let input = opendart::operations::PssrpCptalUseDtls::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("public-offering-proceeds-usage").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::PssrpCptalUseDtlsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::PssrpCptalUseDtlsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("rehabilitation-proceeding-applications" | "DS005-2020021", matches)) => {
            let input = opendart::operations::CtrcvsBgrq::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("rehabilitation-proceeding-applications").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::CtrcvsBgrqJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::CtrcvsBgrqXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("search-disclosures" | "DS001-2019001", matches)) => {
            let mut input = opendart::operations::List::new();
            if let Some(value) = matches.get_one::<String>("corp_code") { input = input.with_corp_code(value.clone()); }
            if let Some(value) = matches.get_one::<String>("bgn_de") { input = input.with_bgn_de(value.clone()); }
            if let Some(value) = matches.get_one::<String>("end_de") { input = input.with_end_de(value.clone()); }
            if let Some(value) = matches.get_one::<String>("last_reprt_at") { input = input.with_last_reprt_at(value.clone()); }
            if let Some(value) = matches.get_one::<String>("pblntf_ty") { input = input.with_pblntf_ty(value.clone()); }
            if let Some(value) = matches.get_one::<String>("pblntf_detail_ty") { input = input.with_pblntf_detail_ty(value.clone()); }
            if let Some(value) = matches.get_one::<String>("corp_cls") { input = input.with_corp_cls(value.clone()); }
            if let Some(value) = matches.get_one::<String>("sort") { input = input.with_sort(value.clone()); }
            if let Some(value) = matches.get_one::<String>("sort_mth") { input = input.with_sort_mth(value.clone()); }
            if let Some(value) = matches.get_one::<String>("page_no") { input = input.with_page_no(value.clone()); }
            if let Some(value) = matches.get_one::<String>("page_count") { input = input.with_page_count(value.clone()); }
            let operation = operation("search-disclosures").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::ListJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::ListXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("share-exchange-transfer-decisions" | "DS005-2020053", matches)) => {
            let input = opendart::operations::StkExtrDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("share-exchange-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::StkExtrDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::StkExtrDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("share-exchange-transfer-registrations" | "DS006-2020058", matches)) => {
            let input = opendart::operations::ExtrRs::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("share-exchange-transfer-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::ExtrRsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::ExtrRsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("shareholder-approved-director-auditor-compensation" | "DS002-2020014", matches)) => {
            let input = opendart::operations::DrctrAdtAllMendngSttusGmtsckConfmAmount::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("shareholder-approved-director-auditor-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::DrctrAdtAllMendngSttusGmtsckConfmAmountJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::DrctrAdtAllMendngSttusGmtsckConfmAmountXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("short-term-bond-outstanding-balance" | "DS002-2020005", matches)) => {
            let input = opendart::operations::SrtpdPsndbtNrdmpBlce::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("short-term-bond-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::SrtpdPsndbtNrdmpBlceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::SrtpdPsndbtNrdmpBlceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("stock-related-bond-acquisition-decisions" | "DS005-2020048", matches)) => {
            let input = opendart::operations::StkrtbdInhDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("stock-related-bond-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::StkrtbdInhDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::StkrtbdInhDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("stock-related-bond-transfer-decisions" | "DS005-2020049", matches)) => {
            let input = opendart::operations::StkrtbdTrfDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("stock-related-bond-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::StkrtbdTrfDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::StkrtbdTrfDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("tangible-asset-acquisition-decisions" | "DS005-2020044", matches)) => {
            let input = opendart::operations::TgastInhDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("tangible-asset-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::TgastInhDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::TgastInhDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("tangible-asset-transfer-decisions" | "DS005-2020045", matches)) => {
            let input = opendart::operations::TgastTrfDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("tangible-asset-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::TgastTrfDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::TgastTrfDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("top-five-individual-compensation" | "DS002-2019014", matches)) => {
            let input = opendart::operations::IndvdlByPay::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("top-five-individual-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::IndvdlByPayJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::IndvdlByPayXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("top-five-individual-compensation-v2" | "DS002-2026002", matches)) => {
            let input = opendart::operations::IndvdlByPayV2::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("top-five-individual-compensation-v2").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::IndvdlByPayV2JsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::IndvdlByPayV2XmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("total-shares-status" | "DS002-2020002", matches)) => {
            let input = opendart::operations::StockTotqySttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("total-shares-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::StockTotqySttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::StockTotqySttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-acquisition-decisions" | "DS005-2020038", matches)) => {
            let input = opendart::operations::TsstkAqDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("treasury-stock-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::TsstkAqDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::TsstkAqDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-acquisition-trust-contract-execution-decisions" | "DS005-2020040", matches)) => {
            let input = opendart::operations::TsstkAqTrctrCnsDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("treasury-stock-acquisition-trust-contract-execution-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::TsstkAqTrctrCnsDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::TsstkAqTrctrCnsDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-acquisition-trust-contract-termination-decisions" | "DS005-2020041", matches)) => {
            let input = opendart::operations::TsstkAqTrctrCcDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("treasury-stock-acquisition-trust-contract-termination-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::TsstkAqTrctrCcDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::TsstkAqTrctrCcDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-disposal-decisions" | "DS005-2020039", matches)) => {
            let input = opendart::operations::TsstkDpDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("treasury-stock-disposal-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::TsstkDpDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::TsstkDpDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-transactions" | "DS002-2019006", matches)) => {
            let input = opendart::operations::TesstkAcqsDspsSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("treasury-stock-transactions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::TesstkAcqsDspsSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::TesstkAcqsDspsSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("unregistered-executive-compensation" | "DS002-2020013", matches)) => {
            let input = opendart::operations::UnrstExctvMendngSttus::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("unregistered-executive-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::UnrstExctvMendngSttusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::UnrstExctvMendngSttusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("write-down-contingent-capital-securities-issuance-decisions" | "DS005-2020037", matches)) => {
            let input = opendart::operations::WdCocobdIsDecsn::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("write-down-contingent-capital-securities-issuance-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::WdCocobdIsDecsnJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::WdCocobdIsDecsnXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("xbrl-taxonomy" | "DS003-2020001", matches)) => {
            let input = opendart::operations::XbrlTaxonomy::new(matches.get_one::<String>("sj_div").expect("clap requires input").clone());
            let operation = operation("xbrl-taxonomy").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::responses::XbrlTaxonomyJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::responses::XbrlTaxonomyXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        _ => unreachable!("clap requires one generated operation"),
    }
}
