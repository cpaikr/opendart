// @generated by opendart-tool; DO NOT EDIT.
// cli-dispatch-generator-schema: 1
// cli-dispatch-projection-sha256: d0cbd0cb9247a0a80d66dff186afb8dfe294b9d334b67a2af7085c580db5d7b5

use clap::ArgMatches;
use opendart::PrepareError;

use crate::interface_projection::catalog::operation;
use crate::prepared::{self, PreparedCall};

pub(crate) fn prepare_call(matches: &ArgMatches) -> Result<PreparedCall, PrepareError> {
    match matches.subcommand() {
        Some(("accounting-auditor-name-and-audit-opinion" | "DS002-2020009", matches)) => {
            let input = opendart::operations::annual_report::AccountingAuditorNameAndAuditOpinionInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("accounting-auditor-name-and-audit-opinion").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::AccountingAuditorNameAndAuditOpinionJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::AccountingAuditorNameAndAuditOpinionXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("audit-service-contracts" | "DS002-2020010", matches)) => {
            let input = opendart::operations::annual_report::AuditServiceContractsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("audit-service-contracts").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::AuditServiceContractsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::AuditServiceContractsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("bond-with-warrants-issuance-decisions" | "DS005-2020034", matches)) => {
            let input = opendart::operations::material_event::BondWithWarrantsIssuanceDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("bond-with-warrants-issuance-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BondWithWarrantsIssuanceDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BondWithWarrantsIssuanceDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("bonus-share-issue-decisions" | "DS005-2020024", matches)) => {
            let input = opendart::operations::material_event::BonusShareIssueDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("bonus-share-issue-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BonusShareIssueDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BonusShareIssueDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("business-acquisition-decisions" | "DS005-2020042", matches)) => {
            let input = opendart::operations::material_event::BusinessAcquisitionDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("business-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BusinessAcquisitionDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BusinessAcquisitionDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("business-suspensions" | "DS005-2020020", matches)) => {
            let input = opendart::operations::material_event::BusinessSuspensionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("business-suspensions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BusinessSuspensionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BusinessSuspensionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("business-transfer-decisions" | "DS005-2020043", matches)) => {
            let input = opendart::operations::material_event::BusinessTransferDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("business-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BusinessTransferDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::BusinessTransferDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("capital-increase-and-reduction-status" | "DS002-2019004", matches)) => {
            let input = opendart::operations::annual_report::CapitalIncreaseAndReductionStatusInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("capital-increase-and-reduction-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::CapitalIncreaseAndReductionStatusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::CapitalIncreaseAndReductionStatusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("capital-reduction-decisions" | "DS005-2020026", matches)) => {
            let input = opendart::operations::material_event::CapitalReductionDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("capital-reduction-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CapitalReductionDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CapitalReductionDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("combined-paid-in-and-bonus-share-issue-decisions" | "DS005-2020025", matches)) => {
            let input = opendart::operations::material_event::CombinedPaidInAndBonusShareIssueDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("combined-paid-in-and-bonus-share-issue-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CombinedPaidInAndBonusShareIssueDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CombinedPaidInAndBonusShareIssueDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("commercial-paper-outstanding-balance" | "DS002-2020004", matches)) => {
            let input = opendart::operations::annual_report::CommercialPaperOutstandingBalanceInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("commercial-paper-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::CommercialPaperOutstandingBalanceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::CommercialPaperOutstandingBalanceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("companies-financial-indicators" | "DS003-2022002", matches)) => {
            let input = opendart::operations::financial_statement::CompaniesFinancialIndicatorsInput::new(matches.get_many::<String>("corp_code").expect("clap requires input").cloned().collect::<Vec<_>>(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone(), matches.get_one::<String>("idx_cl_code").expect("clap requires input").clone());
            let operation = operation("companies-financial-indicators").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompaniesFinancialIndicatorsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompaniesFinancialIndicatorsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("companies-key-accounts" | "DS003-2019017", matches)) => {
            let input = opendart::operations::financial_statement::CompaniesKeyAccountsInput::new(matches.get_many::<String>("corp_code").expect("clap requires input").cloned().collect::<Vec<_>>(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("companies-key-accounts").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompaniesKeyAccountsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompaniesKeyAccountsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-financial-indicators" | "DS003-2022001", matches)) => {
            let input = opendart::operations::financial_statement::CompanyFinancialIndicatorsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone(), matches.get_one::<String>("idx_cl_code").expect("clap requires input").clone());
            let operation = operation("company-financial-indicators").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompanyFinancialIndicatorsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompanyFinancialIndicatorsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-financial-statements" | "DS003-2019020", matches)) => {
            let input = opendart::operations::financial_statement::CompanyFinancialStatementsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone(), matches.get_one::<String>("fs_div").expect("clap requires input").clone());
            let operation = operation("company-financial-statements").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompanyFinancialStatementsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompanyFinancialStatementsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-key-accounts" | "DS003-2019016", matches)) => {
            let input = opendart::operations::financial_statement::CompanyKeyAccountsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("company-key-accounts").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompanyKeyAccountsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::CompanyKeyAccountsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-merger-decisions" | "DS005-2020050", matches)) => {
            let input = opendart::operations::material_event::CompanyMergerDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("company-merger-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CompanyMergerDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CompanyMergerDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-overview" | "DS001-2019002", matches)) => {
            let input = opendart::operations::disclosure::CompanyOverviewInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone());
            let operation = operation("company-overview").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::disclosure::CompanyOverviewJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::disclosure::CompanyOverviewXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-split-decisions" | "DS005-2020051", matches)) => {
            let input = opendart::operations::material_event::CompanySplitDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("company-split-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CompanySplitDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CompanySplitDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("company-split-merger-decisions" | "DS005-2020052", matches)) => {
            let input = opendart::operations::material_event::CompanySplitMergerDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("company-split-merger-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CompanySplitMergerDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CompanySplitMergerDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("contingent-capital-securities-outstanding-balance" | "DS002-2020008", matches)) => {
            let input = opendart::operations::annual_report::ContingentCapitalSecuritiesOutstandingBalanceInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("contingent-capital-securities-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::ContingentCapitalSecuritiesOutstandingBalanceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::ContingentCapitalSecuritiesOutstandingBalanceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("convertible-bond-issuance-decisions" | "DS005-2020033", matches)) => {
            let input = opendart::operations::material_event::ConvertibleBondIssuanceDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("convertible-bond-issuance-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::ConvertibleBondIssuanceDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::ConvertibleBondIssuanceDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("corporate-bond-outstanding-balance" | "DS002-2020006", matches)) => {
            let input = opendart::operations::annual_report::CorporateBondOutstandingBalanceInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("corporate-bond-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::CorporateBondOutstandingBalanceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::CorporateBondOutstandingBalanceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("creditor-bank-management-commencements" | "DS005-2020027", matches)) => {
            let input = opendart::operations::material_event::CreditorBankManagementCommencementsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("creditor-bank-management-commencements").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CreditorBankManagementCommencementsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CreditorBankManagementCommencementsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("creditor-bank-management-discontinuations" | "DS005-2020036", matches)) => {
            let input = opendart::operations::material_event::CreditorBankManagementDiscontinuationsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("creditor-bank-management-discontinuations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CreditorBankManagementDiscontinuationsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::CreditorBankManagementDiscontinuationsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("debt-securities-issuance-results" | "DS002-2020003", matches)) => {
            let input = opendart::operations::annual_report::DebtSecuritiesIssuanceResultsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("debt-securities-issuance-results").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DebtSecuritiesIssuanceResultsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DebtSecuritiesIssuanceResultsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("debt-securities-registrations" | "DS006-2020055", matches)) => {
            let input = opendart::operations::registration_statement::DebtSecuritiesRegistrationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("debt-securities-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::DebtSecuritiesRegistrationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::DebtSecuritiesRegistrationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("default-events" | "DS005-2020019", matches)) => {
            let input = opendart::operations::material_event::DefaultEventsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("default-events").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::DefaultEventsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::DefaultEventsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("demerger-registrations" | "DS006-2020059", matches)) => {
            let input = opendart::operations::registration_statement::DemergerRegistrationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("demerger-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::DemergerRegistrationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::DemergerRegistrationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("depositary-receipt-registrations" | "DS006-2020056", matches)) => {
            let input = opendart::operations::registration_statement::DepositaryReceiptRegistrationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("depositary-receipt-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::DepositaryReceiptRegistrationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::DepositaryReceiptRegistrationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("director-auditor-compensation-by-type" | "DS002-2020015", matches)) => {
            let input = opendart::operations::annual_report::DirectorAuditorCompensationByTypeInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("director-auditor-compensation-by-type").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DirectorAuditorCompensationByTypeJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DirectorAuditorCompensationByTypeXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("director-auditor-individual-compensation" | "DS002-2019012", matches)) => {
            let input = opendart::operations::annual_report::DirectorAuditorIndividualCompensationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("director-auditor-individual-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DirectorAuditorIndividualCompensationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DirectorAuditorIndividualCompensationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("director-auditor-individual-compensation-v2" | "DS002-2026001", matches)) => {
            let input = opendart::operations::annual_report::DirectorAuditorIndividualCompensationV2Input::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("director-auditor-individual-compensation-v2").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DirectorAuditorIndividualCompensationV2JsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DirectorAuditorIndividualCompensationV2XmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("director-auditor-total-compensation" | "DS002-2019013", matches)) => {
            let input = opendart::operations::annual_report::DirectorAuditorTotalCompensationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("director-auditor-total-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DirectorAuditorTotalCompensationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DirectorAuditorTotalCompensationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("dissolution-events" | "DS005-2020022", matches)) => {
            let input = opendart::operations::material_event::DissolutionEventsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("dissolution-events").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::DissolutionEventsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::DissolutionEventsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("dividend-information" | "DS002-2019005", matches)) => {
            let input = opendart::operations::annual_report::DividendInformationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("dividend-information").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DividendInformationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::DividendInformationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("download-company-codes" | "DS001-2019018", _matches)) => {
            let input = opendart::operations::disclosure::CompanyCodesInput::new();
            let operation = operation("download-company-codes").expect("generated catalog entry");
            { let request: opendart::PreparedBinaryRequest = input.prepare_archive()?; Ok(prepared::binary(operation, request)) }
        }
        Some(("download-disclosure-document" | "DS001-2019003", matches)) => {
            let input = opendart::operations::disclosure::DisclosureDocumentInput::new(matches.get_one::<String>("rcept_no").expect("clap requires input").clone());
            let operation = operation("download-disclosure-document").expect("generated catalog entry");
            { let request: opendart::PreparedBinaryRequest = input.prepare_archive()?; Ok(prepared::binary(operation, request)) }
        }
        Some(("download-xbrl-financial-statements" | "DS003-2019019", matches)) => {
            let input = opendart::operations::financial_statement::XbrlFinancialStatementsInput::new(matches.get_one::<String>("rcept_no").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("download-xbrl-financial-statements").expect("generated catalog entry");
            { let request: opendart::PreparedBinaryRequest = input.prepare_archive()?; Ok(prepared::binary(operation, request)) }
        }
        Some(("employee-status" | "DS002-2019011", matches)) => {
            let input = opendart::operations::annual_report::EmployeeStatusInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("employee-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::EmployeeStatusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::EmployeeStatusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("equity-securities-registrations" | "DS006-2020054", matches)) => {
            let input = opendart::operations::registration_statement::EquitySecuritiesRegistrationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("equity-securities-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::EquitySecuritiesRegistrationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::EquitySecuritiesRegistrationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("exchangeable-bond-issuance-decisions" | "DS005-2020035", matches)) => {
            let input = opendart::operations::material_event::ExchangeableBondIssuanceDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("exchangeable-bond-issuance-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::ExchangeableBondIssuanceDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::ExchangeableBondIssuanceDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("executive-status" | "DS002-2019010", matches)) => {
            let input = opendart::operations::annual_report::ExecutiveStatusInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("executive-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::ExecutiveStatusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::ExecutiveStatusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("hybrid-capital-securities-outstanding-balance" | "DS002-2020007", matches)) => {
            let input = opendart::operations::annual_report::HybridCapitalSecuritiesOutstandingBalanceInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("hybrid-capital-securities-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::HybridCapitalSecuritiesOutstandingBalanceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::HybridCapitalSecuritiesOutstandingBalanceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("insider-shareholdings" | "DS004-2019022", matches)) => {
            let input = opendart::operations::ownership::InsiderShareholdingsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone());
            let operation = operation("insider-shareholdings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::ownership::InsiderShareholdingsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::ownership::InsiderShareholdingsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("largest-shareholder-changes" | "DS002-2019008", matches)) => {
            let input = opendart::operations::annual_report::LargestShareholderChangesInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("largest-shareholder-changes").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::LargestShareholderChangesJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::LargestShareholderChangesXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("largest-shareholder-status" | "DS002-2019007", matches)) => {
            let input = opendart::operations::annual_report::LargestShareholderStatusInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("largest-shareholder-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::LargestShareholderStatusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::LargestShareholderStatusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("lawsuit-filings" | "DS005-2020028", matches)) => {
            let input = opendart::operations::material_event::LawsuitFilingsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("lawsuit-filings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::LawsuitFilingsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::LawsuitFilingsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("major-shareholdings" | "DS004-2019021", matches)) => {
            let input = opendart::operations::ownership::MajorShareholdingsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone());
            let operation = operation("major-shareholdings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::ownership::MajorShareholdingsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::ownership::MajorShareholdingsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("merger-registrations" | "DS006-2020057", matches)) => {
            let input = opendart::operations::registration_statement::MergerRegistrationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("merger-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::MergerRegistrationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::MergerRegistrationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("minority-shareholder-status" | "DS002-2019009", matches)) => {
            let input = opendart::operations::annual_report::MinorityShareholderStatusInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("minority-shareholder-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::MinorityShareholderStatusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::MinorityShareholderStatusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("non-audit-service-contracts" | "DS002-2020011", matches)) => {
            let input = opendart::operations::annual_report::NonAuditServiceContractsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("non-audit-service-contracts").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::NonAuditServiceContractsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::NonAuditServiceContractsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("other-asset-transactions-and-putback-options" | "DS005-2020018", matches)) => {
            let input = opendart::operations::material_event::OtherAssetTransactionsAndPutbackOptionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("other-asset-transactions-and-putback-options").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OtherAssetTransactionsAndPutbackOptionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OtherAssetTransactionsAndPutbackOptionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("other-company-equity-securities-acquisition-decisions" | "DS005-2020046", matches)) => {
            let input = opendart::operations::material_event::OtherCompanyEquitySecuritiesAcquisitionDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("other-company-equity-securities-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OtherCompanyEquitySecuritiesAcquisitionDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OtherCompanyEquitySecuritiesAcquisitionDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("other-company-equity-securities-transfer-decisions" | "DS005-2020047", matches)) => {
            let input = opendart::operations::material_event::OtherCompanyEquitySecuritiesTransferDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("other-company-equity-securities-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OtherCompanyEquitySecuritiesTransferDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OtherCompanyEquitySecuritiesTransferDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("other-corporation-investment-status" | "DS002-2019015", matches)) => {
            let input = opendart::operations::annual_report::OtherCorporationInvestmentStatusInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("other-corporation-investment-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::OtherCorporationInvestmentStatusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::OtherCorporationInvestmentStatusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("outside-director-status-and-changes" | "DS002-2020012", matches)) => {
            let input = opendart::operations::annual_report::OutsideDirectorStatusAndChangesInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("outside-director-status-and-changes").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::OutsideDirectorStatusAndChangesJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::OutsideDirectorStatusAndChangesXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("overseas-market-delisting-decisions" | "DS005-2020030", matches)) => {
            let input = opendart::operations::material_event::OverseasMarketDelistingDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("overseas-market-delisting-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OverseasMarketDelistingDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OverseasMarketDelistingDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("overseas-market-delistings" | "DS005-2020032", matches)) => {
            let input = opendart::operations::material_event::OverseasMarketDelistingsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("overseas-market-delistings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OverseasMarketDelistingsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OverseasMarketDelistingsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("overseas-market-listing-decisions" | "DS005-2020029", matches)) => {
            let input = opendart::operations::material_event::OverseasMarketListingDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("overseas-market-listing-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OverseasMarketListingDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OverseasMarketListingDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("overseas-market-listings" | "DS005-2020031", matches)) => {
            let input = opendart::operations::material_event::OverseasMarketListingsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("overseas-market-listings").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OverseasMarketListingsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::OverseasMarketListingsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("paid-in-capital-increase-decisions" | "DS005-2020023", matches)) => {
            let input = opendart::operations::material_event::PaidInCapitalIncreaseDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("paid-in-capital-increase-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::PaidInCapitalIncreaseDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::PaidInCapitalIncreaseDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("private-placement-proceeds-usage" | "DS002-2020017", matches)) => {
            let input = opendart::operations::annual_report::PrivatePlacementProceedsUsageInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("private-placement-proceeds-usage").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::PrivatePlacementProceedsUsageJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::PrivatePlacementProceedsUsageXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("public-offering-proceeds-usage" | "DS002-2020016", matches)) => {
            let input = opendart::operations::annual_report::PublicOfferingProceedsUsageInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("public-offering-proceeds-usage").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::PublicOfferingProceedsUsageJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::PublicOfferingProceedsUsageXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("rehabilitation-proceeding-applications" | "DS005-2020021", matches)) => {
            let input = opendart::operations::material_event::RehabilitationProceedingApplicationsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("rehabilitation-proceeding-applications").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::RehabilitationProceedingApplicationsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::RehabilitationProceedingApplicationsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("search-disclosures" | "DS001-2019001", matches)) => {
            let mut input = opendart::operations::disclosure::DisclosureSearchInput::new();
            if let Some(value) = matches.get_one::<String>("corp_code") { input = input.with_company_code(value.clone()); }
            if let Some(value) = matches.get_one::<String>("bgn_de") { input = input.with_start_date(value.clone()); }
            if let Some(value) = matches.get_one::<String>("end_de") { input = input.with_end_date(value.clone()); }
            if let Some(value) = matches.get_one::<String>("last_reprt_at") { input = input.with_final_reports_only(value.clone()); }
            if let Some(value) = matches.get_one::<String>("pblntf_ty") { input = input.with_disclosure_type(value.clone()); }
            if let Some(value) = matches.get_one::<String>("pblntf_detail_ty") { input = input.with_disclosure_detail_type(value.clone()); }
            if let Some(value) = matches.get_one::<String>("corp_cls") { input = input.with_company_class(value.clone()); }
            if let Some(value) = matches.get_one::<String>("sort") { input = input.with_sort_by(value.clone()); }
            if let Some(value) = matches.get_one::<String>("sort_mth") { input = input.with_sort_order(value.clone()); }
            if let Some(value) = matches.get_one::<String>("page_no") { input = input.with_page(value.clone()); }
            if let Some(value) = matches.get_one::<String>("page_count") { input = input.with_page_size(value.clone()); }
            let operation = operation("search-disclosures").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::disclosure::DisclosureSearchJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::disclosure::DisclosureSearchXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("share-exchange-transfer-decisions" | "DS005-2020053", matches)) => {
            let input = opendart::operations::material_event::ShareExchangeTransferDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("share-exchange-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::ShareExchangeTransferDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::ShareExchangeTransferDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("share-exchange-transfer-registrations" | "DS006-2020058", matches)) => {
            let input = opendart::operations::registration_statement::ShareExchangeTransferRegistrationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("share-exchange-transfer-registrations").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::ShareExchangeTransferRegistrationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::registration_statement::ShareExchangeTransferRegistrationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("shareholder-approved-director-auditor-compensation" | "DS002-2020014", matches)) => {
            let input = opendart::operations::annual_report::ShareholderApprovedDirectorAuditorCompensationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("shareholder-approved-director-auditor-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::ShareholderApprovedDirectorAuditorCompensationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::ShareholderApprovedDirectorAuditorCompensationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("short-term-bond-outstanding-balance" | "DS002-2020005", matches)) => {
            let input = opendart::operations::annual_report::ShortTermBondOutstandingBalanceInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("short-term-bond-outstanding-balance").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::ShortTermBondOutstandingBalanceJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::ShortTermBondOutstandingBalanceXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("stock-related-bond-acquisition-decisions" | "DS005-2020048", matches)) => {
            let input = opendart::operations::material_event::StockRelatedBondAcquisitionDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("stock-related-bond-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::StockRelatedBondAcquisitionDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::StockRelatedBondAcquisitionDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("stock-related-bond-transfer-decisions" | "DS005-2020049", matches)) => {
            let input = opendart::operations::material_event::StockRelatedBondTransferDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("stock-related-bond-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::StockRelatedBondTransferDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::StockRelatedBondTransferDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("tangible-asset-acquisition-decisions" | "DS005-2020044", matches)) => {
            let input = opendart::operations::material_event::TangibleAssetAcquisitionDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("tangible-asset-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TangibleAssetAcquisitionDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TangibleAssetAcquisitionDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("tangible-asset-transfer-decisions" | "DS005-2020045", matches)) => {
            let input = opendart::operations::material_event::TangibleAssetTransferDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("tangible-asset-transfer-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TangibleAssetTransferDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TangibleAssetTransferDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("top-five-individual-compensation" | "DS002-2019014", matches)) => {
            let input = opendart::operations::annual_report::TopFiveIndividualCompensationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("top-five-individual-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::TopFiveIndividualCompensationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::TopFiveIndividualCompensationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("top-five-individual-compensation-v2" | "DS002-2026002", matches)) => {
            let input = opendart::operations::annual_report::TopFiveIndividualCompensationV2Input::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("top-five-individual-compensation-v2").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::TopFiveIndividualCompensationV2JsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::TopFiveIndividualCompensationV2XmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("total-shares-status" | "DS002-2020002", matches)) => {
            let input = opendart::operations::annual_report::TotalSharesStatusInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("total-shares-status").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::TotalSharesStatusJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::TotalSharesStatusXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-acquisition-decisions" | "DS005-2020038", matches)) => {
            let input = opendart::operations::material_event::TreasuryStockAcquisitionDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("treasury-stock-acquisition-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TreasuryStockAcquisitionDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TreasuryStockAcquisitionDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-acquisition-trust-contract-execution-decisions" | "DS005-2020040", matches)) => {
            let input = opendart::operations::material_event::TreasuryStockAcquisitionTrustContractExecutionDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("treasury-stock-acquisition-trust-contract-execution-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TreasuryStockAcquisitionTrustContractExecutionDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TreasuryStockAcquisitionTrustContractExecutionDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-acquisition-trust-contract-termination-decisions" | "DS005-2020041", matches)) => {
            let input = opendart::operations::material_event::TreasuryStockAcquisitionTrustContractTerminationDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("treasury-stock-acquisition-trust-contract-termination-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TreasuryStockAcquisitionTrustContractTerminationDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TreasuryStockAcquisitionTrustContractTerminationDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-disposal-decisions" | "DS005-2020039", matches)) => {
            let input = opendart::operations::material_event::TreasuryStockDisposalDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("treasury-stock-disposal-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TreasuryStockDisposalDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::TreasuryStockDisposalDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("treasury-stock-transactions" | "DS002-2019006", matches)) => {
            let input = opendart::operations::annual_report::TreasuryStockTransactionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("treasury-stock-transactions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::TreasuryStockTransactionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::TreasuryStockTransactionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("unregistered-executive-compensation" | "DS002-2020013", matches)) => {
            let input = opendart::operations::annual_report::UnregisteredExecutiveCompensationInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bsns_year").expect("clap requires input").clone(), matches.get_one::<String>("reprt_code").expect("clap requires input").clone());
            let operation = operation("unregistered-executive-compensation").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::UnregisteredExecutiveCompensationJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::annual_report::UnregisteredExecutiveCompensationXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("write-down-contingent-capital-securities-issuance-decisions" | "DS005-2020037", matches)) => {
            let input = opendart::operations::material_event::WriteDownContingentCapitalSecuritiesIssuanceDecisionsInput::new(matches.get_one::<String>("corp_code").expect("clap requires input").clone(), matches.get_one::<String>("bgn_de").expect("clap requires input").clone(), matches.get_one::<String>("end_de").expect("clap requires input").clone());
            let operation = operation("write-down-contingent-capital-securities-issuance-decisions").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::material_event::WriteDownContingentCapitalSecuritiesIssuanceDecisionsJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::material_event::WriteDownContingentCapitalSecuritiesIssuanceDecisionsXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        Some(("xbrl-taxonomy" | "DS003-2020001", matches)) => {
            let input = opendart::operations::financial_statement::XbrlTaxonomyInput::new(matches.get_one::<String>("sj_div").expect("clap requires input").clone());
            let operation = operation("xbrl-taxonomy").expect("generated catalog entry");
            match matches.get_one::<String>("representation").map(String::as_str) {
                Some("json") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::XbrlTaxonomyJsonResponse> = input.prepare_json()?; Ok(prepared::structured(operation, request)) },
                Some("xml") => { let request: opendart::PreparedRequest<opendart::operations::financial_statement::XbrlTaxonomyXmlResponse> = input.prepare_xml()?; Ok(prepared::structured(operation, request)) },
                _ => unreachable!("clap validates representation"),
            }
        }
        _ => unreachable!("clap requires one generated operation"),
    }
}
