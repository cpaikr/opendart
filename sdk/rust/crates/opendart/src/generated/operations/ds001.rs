// @generated by opendart-tool; DO NOT EDIT.
// generator-schema: 2
// sdk-projection-sha256: ab9e0133bace84eec3b4c3ece7664bc45355f07bc728cc51b848a22209c6a7c4

use crate::request::{QueryParameter, QueryValue, RequestParts};
use crate::{OperationIdentity, PrepareError, PreparedBinaryRequest, PreparedRequest, Representation};
use super::super::responses::ds001 as response;
use super::super::{GENERATOR_SCHEMA, PROJECTION_CHECKSUM};

const JSON_ONLY: &[Representation] = &[Representation::Json];
const XML_ONLY: &[Representation] = &[Representation::Xml];
const ZIP_OR_XML: &[Representation] = &[Representation::Zip, Representation::Xml];

/// OpenDART logical operation `DS001-2019001` (API ID `2019001`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019001>
#[derive(Clone, Debug, Default, Eq, PartialEq)]
pub struct List {
    corp_code: Option<String>,
    bgn_de: Option<String>,
    end_de: Option<String>,
    last_reprt_at: Option<String>,
    pblntf_ty: Option<String>,
    pblntf_detail_ty: Option<String>,
    corp_cls: Option<String>,
    sort: Option<String>,
    sort_mth: Option<String>,
    page_no: Option<String>,
    page_count: Option<String>,
}

impl List {
    const LOGICAL_OPERATION_ID: &'static str = "DS001-2019001";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new() -> Self {
        Self {
            corp_code: None,
            bgn_de: None,
            end_de: None,
            last_reprt_at: None,
            pblntf_ty: None,
            pblntf_detail_ty: None,
            corp_cls: None,
            sort: None,
            sort_mth: None,
            page_no: None,
            page_count: None,
        }
    }

    /// Sets the optional `corp_code` input.
    #[must_use]
    pub fn with_corp_code(mut self, value: impl Into<String>) -> Self { self.corp_code = Some(value.into()); self }

    /// Returns the optional `corp_code` input when supplied.
    #[must_use]
    pub fn corp_code(&self) -> Option<&str> { self.corp_code.as_deref() }

    /// Sets the optional `bgn_de` input.
    #[must_use]
    pub fn with_bgn_de(mut self, value: impl Into<String>) -> Self { self.bgn_de = Some(value.into()); self }

    /// Returns the optional `bgn_de` input when supplied.
    #[must_use]
    pub fn bgn_de(&self) -> Option<&str> { self.bgn_de.as_deref() }

    /// Sets the optional `end_de` input.
    #[must_use]
    pub fn with_end_de(mut self, value: impl Into<String>) -> Self { self.end_de = Some(value.into()); self }

    /// Returns the optional `end_de` input when supplied.
    #[must_use]
    pub fn end_de(&self) -> Option<&str> { self.end_de.as_deref() }

    /// Sets the optional `last_reprt_at` input.
    #[must_use]
    pub fn with_last_reprt_at(mut self, value: impl Into<String>) -> Self { self.last_reprt_at = Some(value.into()); self }

    /// Returns the optional `last_reprt_at` input when supplied.
    #[must_use]
    pub fn last_reprt_at(&self) -> Option<&str> { self.last_reprt_at.as_deref() }

    /// Sets the optional `pblntf_ty` input.
    #[must_use]
    pub fn with_pblntf_ty(mut self, value: impl Into<String>) -> Self { self.pblntf_ty = Some(value.into()); self }

    /// Returns the optional `pblntf_ty` input when supplied.
    #[must_use]
    pub fn pblntf_ty(&self) -> Option<&str> { self.pblntf_ty.as_deref() }

    /// Sets the optional `pblntf_detail_ty` input.
    #[must_use]
    pub fn with_pblntf_detail_ty(mut self, value: impl Into<String>) -> Self { self.pblntf_detail_ty = Some(value.into()); self }

    /// Returns the optional `pblntf_detail_ty` input when supplied.
    #[must_use]
    pub fn pblntf_detail_ty(&self) -> Option<&str> { self.pblntf_detail_ty.as_deref() }

    /// Sets the optional `corp_cls` input.
    #[must_use]
    pub fn with_corp_cls(mut self, value: impl Into<String>) -> Self { self.corp_cls = Some(value.into()); self }

    /// Returns the optional `corp_cls` input when supplied.
    #[must_use]
    pub fn corp_cls(&self) -> Option<&str> { self.corp_cls.as_deref() }

    /// Sets the optional `sort` input.
    #[must_use]
    pub fn with_sort(mut self, value: impl Into<String>) -> Self { self.sort = Some(value.into()); self }

    /// Returns the optional `sort` input when supplied.
    #[must_use]
    pub fn sort(&self) -> Option<&str> { self.sort.as_deref() }

    /// Sets the optional `sort_mth` input.
    #[must_use]
    pub fn with_sort_mth(mut self, value: impl Into<String>) -> Self { self.sort_mth = Some(value.into()); self }

    /// Returns the optional `sort_mth` input when supplied.
    #[must_use]
    pub fn sort_mth(&self) -> Option<&str> { self.sort_mth.as_deref() }

    /// Sets the optional `page_no` input.
    #[must_use]
    pub fn with_page_no(mut self, value: impl Into<String>) -> Self { self.page_no = Some(value.into()); self }

    /// Returns the optional `page_no` input when supplied.
    #[must_use]
    pub fn page_no(&self) -> Option<&str> { self.page_no.as_deref() }

    /// Sets the optional `page_count` input.
    #[must_use]
    pub fn with_page_count(mut self, value: impl Into<String>) -> Self { self.page_count = Some(value.into()); self }

    /// Returns the optional `page_count` input when supplied.
    #[must_use]
    pub fn page_count(&self) -> Option<&str> { self.page_count.as_deref() }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::ListJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_list_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/list.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_list_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::ListXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_list_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/list.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_list_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        if let Some(value) = &self.corp_code { require_nonempty(identity, "corp_code", value)?; }
        if let Some(value) = &self.bgn_de { require_nonempty(identity, "bgn_de", value)?; }
        if let Some(value) = &self.end_de { require_nonempty(identity, "end_de", value)?; }
        if let Some(value) = &self.last_reprt_at { require_nonempty(identity, "last_reprt_at", value)?; }
        if let Some(value) = &self.pblntf_ty { require_nonempty(identity, "pblntf_ty", value)?; }
        if let Some(value) = &self.pblntf_detail_ty { require_nonempty(identity, "pblntf_detail_ty", value)?; }
        if let Some(value) = &self.corp_cls { require_nonempty(identity, "corp_cls", value)?; }
        if let Some(value) = &self.sort { require_nonempty(identity, "sort", value)?; }
        if let Some(value) = &self.sort_mth { require_nonempty(identity, "sort_mth", value)?; }
        if let Some(value) = &self.page_no { require_nonempty(identity, "page_no", value)?; }
        if let Some(value) = &self.page_count { require_nonempty(identity, "page_count", value)?; }
        let mut parameters = Vec::with_capacity(11);
        if let Some(value) = &self.corp_code { parameters.push(QueryParameter { name: "corp_code", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.bgn_de { parameters.push(QueryParameter { name: "bgn_de", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.end_de { parameters.push(QueryParameter { name: "end_de", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.last_reprt_at { parameters.push(QueryParameter { name: "last_reprt_at", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.pblntf_ty { parameters.push(QueryParameter { name: "pblntf_ty", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.pblntf_detail_ty { parameters.push(QueryParameter { name: "pblntf_detail_ty", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.corp_cls { parameters.push(QueryParameter { name: "corp_cls", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.sort { parameters.push(QueryParameter { name: "sort", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.sort_mth { parameters.push(QueryParameter { name: "sort_mth", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.page_no { parameters.push(QueryParameter { name: "page_no", value: QueryValue::Scalar(value) }); }
        if let Some(value) = &self.page_count { parameters.push(QueryParameter { name: "page_count", value: QueryValue::Scalar(value) }); }
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS001-2019002` (API ID `2019002`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019002>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct Company {
    corp_code: String,
}

impl Company {
    const LOGICAL_OPERATION_ID: &'static str = "DS001-2019002";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::CompanyJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_company_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/company.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_company_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::CompanyXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_company_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/company.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_company_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS001-2019003` (API ID `2019003`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019003>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct Document {
    rcept_no: String,
}

impl Document {
    const LOGICAL_OPERATION_ID: &'static str = "DS001-2019003";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(rcept_no: impl Into<String>) -> Self {
        Self {
            rcept_no: rcept_no.into(),
        }
    }

    /// Returns the `rcept_no` input.
    #[must_use]
    pub fn rcept_no(&self) -> &str { &self.rcept_no }

    /// Prepares the ZIP physical representation without performing I/O.
    pub fn prepare_zip(&self) -> Result<PreparedBinaryRequest, PrepareError> {
        let identity = OperationIdentity::new("get_document_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/document.xml", identity, ZIP_OR_XML)?;
        Ok(PreparedBinaryRequest::new(parts))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "rcept_no", &self.rcept_no)?;
        let parameters = vec![
            QueryParameter { name: "rcept_no", value: QueryValue::Scalar(self.rcept_no.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS001-2019018` (API ID `2019018`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS001&apiId=2019018>
#[derive(Clone, Copy, Debug, Default, Eq, PartialEq)]
pub struct CorpCode;

impl CorpCode {
    const LOGICAL_OPERATION_ID: &'static str = "DS001-2019018";

    /// Creates the parameter-free operation input.
    #[must_use]
    pub const fn new() -> Self { Self }

    /// Prepares the ZIP physical representation without performing I/O.
    pub fn prepare_zip(&self) -> Result<PreparedBinaryRequest, PrepareError> {
        let identity = OperationIdentity::new("get_corpCode_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/corpCode.xml", identity, ZIP_OR_XML)?;
        Ok(PreparedBinaryRequest::new(parts))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        let parameters = [];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

fn require_nonempty(operation: OperationIdentity, parameter: &'static str, value: &str) -> Result<(), PrepareError> {
    if value.is_empty() { return Err(PrepareError::MissingInput { operation, parameter }); }
    Ok(())
}

#[cfg(test)]
mod generated_request_tests {
    use super::*;
    use crate::{Authentication, RequestMethod};

    #[test]
    fn every_physical_request_matches_its_generated_vector() {
        {
            let input = List::new().with_corp_code("value /+").with_bgn_de("value /+").with_end_de("value /+").with_last_reprt_at("value /+").with_pblntf_ty("value /+").with_pblntf_detail_ty("value /+").with_corp_cls("value /+").with_sort("value /+").with_sort_mth("value /+").with_page_no("value /+").with_page_count("value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_list_json");
            assert_eq!(prepared.identity().logical(), "DS001-2019001");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/list.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bgn_de=value+%2F%2B&end_de=value+%2F%2B&last_reprt_at=value+%2F%2B&pblntf_ty=value+%2F%2B&pblntf_detail_ty=value+%2F%2B&corp_cls=value+%2F%2B&sort=value+%2F%2B&sort_mth=value+%2F%2B&page_no=value+%2F%2B&page_count=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
            let defaults = List::new().prepare_json().expect("omitted optional values must prepare");
            assert_eq!(defaults.encoded_query(), "");
        }
        {
            let input = List::new().with_corp_code("value /+").with_bgn_de("value /+").with_end_de("value /+").with_last_reprt_at("value /+").with_pblntf_ty("value /+").with_pblntf_detail_ty("value /+").with_corp_cls("value /+").with_sort("value /+").with_sort_mth("value /+").with_page_no("value /+").with_page_count("value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_list_xml");
            assert_eq!(prepared.identity().logical(), "DS001-2019001");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/list.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bgn_de=value+%2F%2B&end_de=value+%2F%2B&last_reprt_at=value+%2F%2B&pblntf_ty=value+%2F%2B&pblntf_detail_ty=value+%2F%2B&corp_cls=value+%2F%2B&sort=value+%2F%2B&sort_mth=value+%2F%2B&page_no=value+%2F%2B&page_count=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
            let defaults = List::new().prepare_xml().expect("omitted optional values must prepare");
            assert_eq!(defaults.encoded_query(), "");
        }
        {
            let input = Company::new("value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_company_json");
            assert_eq!(prepared.identity().logical(), "DS001-2019002");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/company.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = Company::new("value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_company_xml");
            assert_eq!(prepared.identity().logical(), "DS001-2019002");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/company.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = Document::new("value /+");
            let prepared = input.prepare_zip().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_document_xml");
            assert_eq!(prepared.identity().logical(), "DS001-2019003");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/document.xml");
            assert_eq!(prepared.encoded_query(), "rcept_no=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), ZIP_OR_XML);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = CorpCode::new();
            let prepared = input.prepare_zip().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_corpCode_xml");
            assert_eq!(prepared.identity().logical(), "DS001-2019018");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/corpCode.xml");
            assert_eq!(prepared.encoded_query(), "");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), ZIP_OR_XML);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
    }
}
