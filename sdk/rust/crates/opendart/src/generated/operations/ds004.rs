// @generated by opendart-tool; DO NOT EDIT.
// generator-schema: 1
// sdk-projection-sha256: c22df6ce91eb19359a67b73d736be5fb790b009c7a11708fe5fffbcd643ecaab

use crate::request::{QueryParameter, QueryValue};
use crate::{OperationIdentity, PrepareError, PreparedRequest, Representation};
use super::super::{GENERATOR_SCHEMA, PROJECTION_CHECKSUM};

const JSON_ONLY: &[Representation] = &[Representation::Json];
const XML_ONLY: &[Representation] = &[Representation::Xml];

/// OpenDART logical operation `DS004-2019021` (API ID `2019021`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS004&apiId=2019021>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct Majorstock {
    corp_code: String,
}

impl Majorstock {
    const LOGICAL_OPERATION_ID: &'static str = "DS004-2019021";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Prepares one physical representation without performing I/O.
    pub fn prepare(&self, representation: Representation) -> Result<PreparedRequest, PrepareError> {
        let (path, identity, expected) = match representation {
            Representation::Json => ("/api/majorstock.json", OperationIdentity::new("get_majorstock_json", Self::LOGICAL_OPERATION_ID), JSON_ONLY),
            Representation::Xml => ("/api/majorstock.xml", OperationIdentity::new("get_majorstock_xml", Self::LOGICAL_OPERATION_ID), XML_ONLY),
            _ => return Err(PrepareError::UnsupportedRepresentation { logical_operation: Self::LOGICAL_OPERATION_ID, representation }),
        };
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
        ];
        Ok(PreparedRequest::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS004-2019022` (API ID `2019022`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS004&apiId=2019022>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct Elestock {
    corp_code: String,
}

impl Elestock {
    const LOGICAL_OPERATION_ID: &'static str = "DS004-2019022";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Prepares one physical representation without performing I/O.
    pub fn prepare(&self, representation: Representation) -> Result<PreparedRequest, PrepareError> {
        let (path, identity, expected) = match representation {
            Representation::Json => ("/api/elestock.json", OperationIdentity::new("get_elestock_json", Self::LOGICAL_OPERATION_ID), JSON_ONLY),
            Representation::Xml => ("/api/elestock.xml", OperationIdentity::new("get_elestock_xml", Self::LOGICAL_OPERATION_ID), XML_ONLY),
            _ => return Err(PrepareError::UnsupportedRepresentation { logical_operation: Self::LOGICAL_OPERATION_ID, representation }),
        };
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
        ];
        Ok(PreparedRequest::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

fn require_nonempty(operation: OperationIdentity, parameter: &'static str, value: &str) -> Result<(), PrepareError> {
    if value.is_empty() { return Err(PrepareError::MissingInput { operation, parameter }); }
    Ok(())
}

#[cfg(test)]
mod generated_request_tests {
    use super::*;
    use crate::{Authentication, RequestMethod};

    #[test]
    fn every_physical_request_matches_its_generated_vector() {
        {
            let input = Majorstock::new("value /+");
            let prepared = input.prepare(Representation::Json).expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_majorstock_json");
            assert_eq!(prepared.identity().logical(), "DS004-2019021");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/majorstock.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = Majorstock::new("value /+");
            let prepared = input.prepare(Representation::Xml).expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_majorstock_xml");
            assert_eq!(prepared.identity().logical(), "DS004-2019021");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/majorstock.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = Elestock::new("value /+");
            let prepared = input.prepare(Representation::Json).expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_elestock_json");
            assert_eq!(prepared.identity().logical(), "DS004-2019022");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/elestock.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = Elestock::new("value /+");
            let prepared = input.prepare(Representation::Xml).expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_elestock_xml");
            assert_eq!(prepared.identity().logical(), "DS004-2019022");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/elestock.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
    }
}
