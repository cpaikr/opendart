// @generated by opendart-tool; DO NOT EDIT.
// generator-schema: 2
// sdk-projection-sha256: ab9e0133bace84eec3b4c3ece7664bc45355f07bc728cc51b848a22209c6a7c4

use crate::request::{QueryParameter, QueryValue, RequestParts};
use crate::{OperationIdentity, PrepareError, PreparedRequest, Representation};
use super::super::responses::ds002 as response;
use super::super::{GENERATOR_SCHEMA, PROJECTION_CHECKSUM};

const JSON_ONLY: &[Representation] = &[Representation::Json];
const XML_ONLY: &[Representation] = &[Representation::Xml];

/// OpenDART logical operation `DS002-2019004` (API ID `2019004`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019004>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct IrdsSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl IrdsSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019004";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::IrdsSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_irdsSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/irdsSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_irds_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::IrdsSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_irdsSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/irdsSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_irds_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019005` (API ID `2019005`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019005>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct AlotMatter {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl AlotMatter {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019005";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::AlotMatterJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_alotMatter_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/alotMatter.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_alot_matter_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::AlotMatterXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_alotMatter_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/alotMatter.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_alot_matter_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019006` (API ID `2019006`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019006>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct TesstkAcqsDspsSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl TesstkAcqsDspsSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019006";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::TesstkAcqsDspsSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_tesstkAcqsDspsSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/tesstkAcqsDspsSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_tesstk_acqs_dsps_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::TesstkAcqsDspsSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_tesstkAcqsDspsSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/tesstkAcqsDspsSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_tesstk_acqs_dsps_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019007` (API ID `2019007`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019007>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct HyslrSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl HyslrSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019007";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::HyslrSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hyslrSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hyslrSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hyslr_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::HyslrSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hyslrSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hyslrSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hyslr_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019008` (API ID `2019008`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019008>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct HyslrChgSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl HyslrChgSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019008";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::HyslrChgSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hyslrChgSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hyslrChgSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hyslr_chg_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::HyslrChgSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hyslrChgSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hyslrChgSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hyslr_chg_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019009` (API ID `2019009`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019009>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct MrhlSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl MrhlSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019009";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::MrhlSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_mrhlSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/mrhlSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_mrhl_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::MrhlSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_mrhlSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/mrhlSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_mrhl_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019010` (API ID `2019010`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019010>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct ExctvSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl ExctvSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019010";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::ExctvSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_exctvSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/exctvSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_exctv_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::ExctvSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_exctvSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/exctvSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_exctv_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019011` (API ID `2019011`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019011>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct EmpSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl EmpSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019011";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::EmpSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_empSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/empSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_emp_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::EmpSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_empSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/empSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_emp_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019012` (API ID `2019012`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019012>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct HmvAuditIndvdlBySttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl HmvAuditIndvdlBySttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019012";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::HmvAuditIndvdlBySttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hmvAuditIndvdlBySttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hmvAuditIndvdlBySttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hmv_audit_indvdl_by_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::HmvAuditIndvdlBySttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hmvAuditIndvdlBySttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hmvAuditIndvdlBySttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hmv_audit_indvdl_by_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019013` (API ID `2019013`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019013>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct HmvAuditAllSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl HmvAuditAllSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019013";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::HmvAuditAllSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hmvAuditAllSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hmvAuditAllSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hmv_audit_all_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::HmvAuditAllSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hmvAuditAllSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hmvAuditAllSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hmv_audit_all_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019014` (API ID `2019014`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019014>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct IndvdlByPay {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl IndvdlByPay {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019014";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::IndvdlByPayJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_indvdlByPay_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/indvdlByPay.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_indvdl_by_pay_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::IndvdlByPayXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_indvdlByPay_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/indvdlByPay.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_indvdl_by_pay_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2019015` (API ID `2019015`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2019015>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct OtrCprInvstmntSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl OtrCprInvstmntSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2019015";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::OtrCprInvstmntSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_otrCprInvstmntSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/otrCprInvstmntSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_otr_cpr_invstmnt_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::OtrCprInvstmntSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_otrCprInvstmntSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/otrCprInvstmntSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_otr_cpr_invstmnt_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020002` (API ID `2020002`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020002>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct StockTotqySttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl StockTotqySttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020002";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::StockTotqySttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_stockTotqySttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/stockTotqySttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_stock_totqy_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::StockTotqySttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_stockTotqySttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/stockTotqySttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_stock_totqy_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020003` (API ID `2020003`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020003>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct DetScritsIsuAcmslt {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl DetScritsIsuAcmslt {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020003";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::DetScritsIsuAcmsltJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_detScritsIsuAcmslt_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/detScritsIsuAcmslt.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_det_scrits_isu_acmslt_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::DetScritsIsuAcmsltXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_detScritsIsuAcmslt_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/detScritsIsuAcmslt.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_det_scrits_isu_acmslt_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020004` (API ID `2020004`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020004>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct EntrprsBilScritsNrdmpBlce {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl EntrprsBilScritsNrdmpBlce {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020004";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::EntrprsBilScritsNrdmpBlceJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_entrprsBilScritsNrdmpBlce_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/entrprsBilScritsNrdmpBlce.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_entrprs_bil_scrits_nrdmp_blce_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::EntrprsBilScritsNrdmpBlceXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_entrprsBilScritsNrdmpBlce_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/entrprsBilScritsNrdmpBlce.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_entrprs_bil_scrits_nrdmp_blce_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020005` (API ID `2020005`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020005>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct SrtpdPsndbtNrdmpBlce {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl SrtpdPsndbtNrdmpBlce {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020005";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::SrtpdPsndbtNrdmpBlceJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_srtpdPsndbtNrdmpBlce_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/srtpdPsndbtNrdmpBlce.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_srtpd_psndbt_nrdmp_blce_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::SrtpdPsndbtNrdmpBlceXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_srtpdPsndbtNrdmpBlce_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/srtpdPsndbtNrdmpBlce.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_srtpd_psndbt_nrdmp_blce_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020006` (API ID `2020006`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020006>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct CprndNrdmpBlce {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl CprndNrdmpBlce {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020006";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::CprndNrdmpBlceJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_cprndNrdmpBlce_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/cprndNrdmpBlce.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_cprnd_nrdmp_blce_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::CprndNrdmpBlceXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_cprndNrdmpBlce_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/cprndNrdmpBlce.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_cprnd_nrdmp_blce_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020007` (API ID `2020007`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020007>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct NewCaplScritsNrdmpBlce {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl NewCaplScritsNrdmpBlce {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020007";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::NewCaplScritsNrdmpBlceJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_newCaplScritsNrdmpBlce_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/newCaplScritsNrdmpBlce.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_new_capl_scrits_nrdmp_blce_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::NewCaplScritsNrdmpBlceXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_newCaplScritsNrdmpBlce_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/newCaplScritsNrdmpBlce.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_new_capl_scrits_nrdmp_blce_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020008` (API ID `2020008`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020008>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct CndlCaplScritsNrdmpBlce {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl CndlCaplScritsNrdmpBlce {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020008";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::CndlCaplScritsNrdmpBlceJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_cndlCaplScritsNrdmpBlce_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/cndlCaplScritsNrdmpBlce.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_cndl_capl_scrits_nrdmp_blce_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::CndlCaplScritsNrdmpBlceXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_cndlCaplScritsNrdmpBlce_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/cndlCaplScritsNrdmpBlce.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_cndl_capl_scrits_nrdmp_blce_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020009` (API ID `2020009`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020009>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct AccnutAdtorNmNdAdtOpinion {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl AccnutAdtorNmNdAdtOpinion {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020009";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::AccnutAdtorNmNdAdtOpinionJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_accnutAdtorNmNdAdtOpinion_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/accnutAdtorNmNdAdtOpinion.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_accnut_adtor_nm_nd_adt_opinion_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::AccnutAdtorNmNdAdtOpinionXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_accnutAdtorNmNdAdtOpinion_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/accnutAdtorNmNdAdtOpinion.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_accnut_adtor_nm_nd_adt_opinion_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020010` (API ID `2020010`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020010>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct AdtServcCnclsSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl AdtServcCnclsSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020010";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::AdtServcCnclsSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_adtServcCnclsSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/adtServcCnclsSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_adt_servc_cncls_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::AdtServcCnclsSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_adtServcCnclsSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/adtServcCnclsSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_adt_servc_cncls_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020011` (API ID `2020011`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020011>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct AccnutAdtorNonAdtServcCnclsSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl AccnutAdtorNonAdtServcCnclsSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020011";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::AccnutAdtorNonAdtServcCnclsSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_accnutAdtorNonAdtServcCnclsSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/accnutAdtorNonAdtServcCnclsSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_accnut_adtor_non_adt_servc_cncls_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::AccnutAdtorNonAdtServcCnclsSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_accnutAdtorNonAdtServcCnclsSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/accnutAdtorNonAdtServcCnclsSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_accnut_adtor_non_adt_servc_cncls_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020012` (API ID `2020012`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020012>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct OutcmpnyDrctrNdChangeSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl OutcmpnyDrctrNdChangeSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020012";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::OutcmpnyDrctrNdChangeSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_outcmpnyDrctrNdChangeSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/outcmpnyDrctrNdChangeSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_outcmpny_drctr_nd_change_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::OutcmpnyDrctrNdChangeSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_outcmpnyDrctrNdChangeSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/outcmpnyDrctrNdChangeSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_outcmpny_drctr_nd_change_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020013` (API ID `2020013`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020013>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct UnrstExctvMendngSttus {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl UnrstExctvMendngSttus {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020013";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::UnrstExctvMendngSttusJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_unrstExctvMendngSttus_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/unrstExctvMendngSttus.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_unrst_exctv_mendng_sttus_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::UnrstExctvMendngSttusXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_unrstExctvMendngSttus_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/unrstExctvMendngSttus.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_unrst_exctv_mendng_sttus_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020014` (API ID `2020014`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020014>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct DrctrAdtAllMendngSttusGmtsckConfmAmount {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl DrctrAdtAllMendngSttusGmtsckConfmAmount {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020014";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::DrctrAdtAllMendngSttusGmtsckConfmAmountJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_drctrAdtAllMendngSttusGmtsckConfmAmount_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/drctrAdtAllMendngSttusGmtsckConfmAmount.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_drctr_adt_all_mendng_sttus_gmtsck_confm_amount_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::DrctrAdtAllMendngSttusGmtsckConfmAmountXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_drctrAdtAllMendngSttusGmtsckConfmAmount_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/drctrAdtAllMendngSttusGmtsckConfmAmount.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_drctr_adt_all_mendng_sttus_gmtsck_confm_amount_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020015` (API ID `2020015`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020015>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct DrctrAdtAllMendngSttusMendngPymntamtTyCl {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl DrctrAdtAllMendngSttusMendngPymntamtTyCl {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020015";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::DrctrAdtAllMendngSttusMendngPymntamtTyClJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_drctrAdtAllMendngSttusMendngPymntamtTyCl_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/drctrAdtAllMendngSttusMendngPymntamtTyCl.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_drctr_adt_all_mendng_sttus_mendng_pymntamt_ty_cl_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::DrctrAdtAllMendngSttusMendngPymntamtTyClXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_drctrAdtAllMendngSttusMendngPymntamtTyCl_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/drctrAdtAllMendngSttusMendngPymntamtTyCl.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_drctr_adt_all_mendng_sttus_mendng_pymntamt_ty_cl_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020016` (API ID `2020016`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020016>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct PssrpCptalUseDtls {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl PssrpCptalUseDtls {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020016";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::PssrpCptalUseDtlsJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_pssrpCptalUseDtls_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/pssrpCptalUseDtls.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_pssrp_cptal_use_dtls_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::PssrpCptalUseDtlsXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_pssrpCptalUseDtls_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/pssrpCptalUseDtls.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_pssrp_cptal_use_dtls_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2020017` (API ID `2020017`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2020017>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct PrvsrpCptalUseDtls {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl PrvsrpCptalUseDtls {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2020017";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::PrvsrpCptalUseDtlsJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_prvsrpCptalUseDtls_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/prvsrpCptalUseDtls.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_prvsrp_cptal_use_dtls_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::PrvsrpCptalUseDtlsXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_prvsrpCptalUseDtls_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/prvsrpCptalUseDtls.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_prvsrp_cptal_use_dtls_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2026001` (API ID `2026001`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2026001>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct HmvAuditIndvdlBySttusV2 {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl HmvAuditIndvdlBySttusV2 {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2026001";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::HmvAuditIndvdlBySttusV2JsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hmvAuditIndvdlBySttusV2_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hmvAuditIndvdlBySttusV2.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hmv_audit_indvdl_by_sttus_v2_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::HmvAuditIndvdlBySttusV2XmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_hmvAuditIndvdlBySttusV2_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/hmvAuditIndvdlBySttusV2.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_hmv_audit_indvdl_by_sttus_v2_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS002-2026002` (API ID `2026002`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS002&apiId=2026002>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct IndvdlByPayV2 {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl IndvdlByPayV2 {
    const LOGICAL_OPERATION_ID: &'static str = "DS002-2026002";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::IndvdlByPayV2JsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_indvdlByPayV2_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/indvdlByPayV2.json", identity, JSON_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_indvdl_by_pay_v2_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::IndvdlByPayV2XmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_indvdlByPayV2_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/indvdlByPayV2.xml", identity, XML_ONLY)?;
        Ok(PreparedRequest::new(parts, response::decode_indvdl_by_pay_v2_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation]) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

fn require_nonempty(operation: OperationIdentity, parameter: &'static str, value: &str) -> Result<(), PrepareError> {
    if value.is_empty() { return Err(PrepareError::MissingInput { operation, parameter }); }
    Ok(())
}

#[cfg(test)]
mod generated_request_tests {
    use super::*;
    use crate::{Authentication, RequestMethod};

    #[test]
    fn every_physical_request_matches_its_generated_vector() {
        {
            let input = IrdsSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_irdsSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019004");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/irdsSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = IrdsSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_irdsSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019004");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/irdsSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = AlotMatter::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_alotMatter_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019005");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/alotMatter.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = AlotMatter::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_alotMatter_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019005");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/alotMatter.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = TesstkAcqsDspsSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_tesstkAcqsDspsSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019006");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/tesstkAcqsDspsSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = TesstkAcqsDspsSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_tesstkAcqsDspsSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019006");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/tesstkAcqsDspsSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HyslrSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hyslrSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019007");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hyslrSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HyslrSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hyslrSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019007");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hyslrSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HyslrChgSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hyslrChgSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019008");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hyslrChgSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HyslrChgSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hyslrChgSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019008");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hyslrChgSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = MrhlSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_mrhlSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019009");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/mrhlSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = MrhlSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_mrhlSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019009");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/mrhlSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = ExctvSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_exctvSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019010");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/exctvSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = ExctvSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_exctvSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019010");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/exctvSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = EmpSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_empSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019011");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/empSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = EmpSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_empSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019011");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/empSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HmvAuditIndvdlBySttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hmvAuditIndvdlBySttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019012");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hmvAuditIndvdlBySttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HmvAuditIndvdlBySttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hmvAuditIndvdlBySttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019012");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hmvAuditIndvdlBySttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HmvAuditAllSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hmvAuditAllSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019013");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hmvAuditAllSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HmvAuditAllSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hmvAuditAllSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019013");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hmvAuditAllSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = IndvdlByPay::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_indvdlByPay_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019014");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/indvdlByPay.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = IndvdlByPay::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_indvdlByPay_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019014");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/indvdlByPay.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = OtrCprInvstmntSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_otrCprInvstmntSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2019015");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/otrCprInvstmntSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = OtrCprInvstmntSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_otrCprInvstmntSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2019015");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/otrCprInvstmntSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = StockTotqySttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_stockTotqySttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020002");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/stockTotqySttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = StockTotqySttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_stockTotqySttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020002");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/stockTotqySttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = DetScritsIsuAcmslt::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_detScritsIsuAcmslt_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020003");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/detScritsIsuAcmslt.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = DetScritsIsuAcmslt::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_detScritsIsuAcmslt_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020003");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/detScritsIsuAcmslt.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = EntrprsBilScritsNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_entrprsBilScritsNrdmpBlce_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020004");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/entrprsBilScritsNrdmpBlce.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = EntrprsBilScritsNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_entrprsBilScritsNrdmpBlce_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020004");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/entrprsBilScritsNrdmpBlce.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = SrtpdPsndbtNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_srtpdPsndbtNrdmpBlce_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020005");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/srtpdPsndbtNrdmpBlce.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = SrtpdPsndbtNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_srtpdPsndbtNrdmpBlce_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020005");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/srtpdPsndbtNrdmpBlce.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = CprndNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_cprndNrdmpBlce_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020006");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/cprndNrdmpBlce.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = CprndNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_cprndNrdmpBlce_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020006");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/cprndNrdmpBlce.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = NewCaplScritsNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_newCaplScritsNrdmpBlce_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020007");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/newCaplScritsNrdmpBlce.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = NewCaplScritsNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_newCaplScritsNrdmpBlce_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020007");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/newCaplScritsNrdmpBlce.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = CndlCaplScritsNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_cndlCaplScritsNrdmpBlce_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020008");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/cndlCaplScritsNrdmpBlce.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = CndlCaplScritsNrdmpBlce::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_cndlCaplScritsNrdmpBlce_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020008");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/cndlCaplScritsNrdmpBlce.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = AccnutAdtorNmNdAdtOpinion::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_accnutAdtorNmNdAdtOpinion_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020009");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/accnutAdtorNmNdAdtOpinion.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = AccnutAdtorNmNdAdtOpinion::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_accnutAdtorNmNdAdtOpinion_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020009");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/accnutAdtorNmNdAdtOpinion.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = AdtServcCnclsSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_adtServcCnclsSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020010");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/adtServcCnclsSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = AdtServcCnclsSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_adtServcCnclsSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020010");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/adtServcCnclsSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = AccnutAdtorNonAdtServcCnclsSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_accnutAdtorNonAdtServcCnclsSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020011");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/accnutAdtorNonAdtServcCnclsSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = AccnutAdtorNonAdtServcCnclsSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_accnutAdtorNonAdtServcCnclsSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020011");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/accnutAdtorNonAdtServcCnclsSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = OutcmpnyDrctrNdChangeSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_outcmpnyDrctrNdChangeSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020012");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/outcmpnyDrctrNdChangeSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = OutcmpnyDrctrNdChangeSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_outcmpnyDrctrNdChangeSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020012");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/outcmpnyDrctrNdChangeSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = UnrstExctvMendngSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_unrstExctvMendngSttus_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020013");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/unrstExctvMendngSttus.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = UnrstExctvMendngSttus::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_unrstExctvMendngSttus_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020013");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/unrstExctvMendngSttus.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = DrctrAdtAllMendngSttusGmtsckConfmAmount::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_drctrAdtAllMendngSttusGmtsckConfmAmount_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020014");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/drctrAdtAllMendngSttusGmtsckConfmAmount.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = DrctrAdtAllMendngSttusGmtsckConfmAmount::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_drctrAdtAllMendngSttusGmtsckConfmAmount_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020014");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/drctrAdtAllMendngSttusGmtsckConfmAmount.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = DrctrAdtAllMendngSttusMendngPymntamtTyCl::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_drctrAdtAllMendngSttusMendngPymntamtTyCl_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020015");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/drctrAdtAllMendngSttusMendngPymntamtTyCl.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = DrctrAdtAllMendngSttusMendngPymntamtTyCl::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_drctrAdtAllMendngSttusMendngPymntamtTyCl_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020015");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/drctrAdtAllMendngSttusMendngPymntamtTyCl.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = PssrpCptalUseDtls::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_pssrpCptalUseDtls_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020016");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/pssrpCptalUseDtls.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = PssrpCptalUseDtls::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_pssrpCptalUseDtls_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020016");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/pssrpCptalUseDtls.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = PrvsrpCptalUseDtls::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_prvsrpCptalUseDtls_json");
            assert_eq!(prepared.identity().logical(), "DS002-2020017");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/prvsrpCptalUseDtls.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = PrvsrpCptalUseDtls::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_prvsrpCptalUseDtls_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2020017");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/prvsrpCptalUseDtls.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HmvAuditIndvdlBySttusV2::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hmvAuditIndvdlBySttusV2_json");
            assert_eq!(prepared.identity().logical(), "DS002-2026001");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hmvAuditIndvdlBySttusV2.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = HmvAuditIndvdlBySttusV2::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_hmvAuditIndvdlBySttusV2_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2026001");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/hmvAuditIndvdlBySttusV2.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = IndvdlByPayV2::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_indvdlByPayV2_json");
            assert_eq!(prepared.identity().logical(), "DS002-2026002");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/indvdlByPayV2.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = IndvdlByPayV2::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_indvdlByPayV2_xml");
            assert_eq!(prepared.identity().logical(), "DS002-2026002");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/indvdlByPayV2.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
    }
}
