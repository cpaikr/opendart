// @generated by opendart-tool; DO NOT EDIT.
// generator-schema: 2
// sdk-projection-sha256: ab9e0133bace84eec3b4c3ece7664bc45355f07bc728cc51b848a22209c6a7c4

use crate::request::{QueryParameter, QueryValue, RequestParts};
use crate::{OperationIdentity, PrepareError, PreparedBinaryRequest, PreparedRequest, Representation};
use super::super::responses::ds003 as response;
use super::super::{GENERATOR_SCHEMA, PROJECTION_CHECKSUM};

const JSON_ONLY: &[Representation] = &[Representation::Json];
const XML_ONLY: &[Representation] = &[Representation::Xml];
const ZIP_OR_XML: &[Representation] = &[Representation::Zip, Representation::Xml];

/// OpenDART logical operation `DS003-2019016` (API ID `2019016`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS003&apiId=2019016>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct FnlttSinglAcnt {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
}

impl FnlttSinglAcnt {
    const LOGICAL_OPERATION_ID: &'static str = "DS003-2019016";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::FnlttSinglAcntJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttSinglAcnt_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttSinglAcnt.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_singl_acnt_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::FnlttSinglAcntXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttSinglAcnt_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttSinglAcnt.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_singl_acnt_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS003-2019017` (API ID `2019017`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS003&apiId=2019017>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct FnlttMultiAcnt {
    corp_code: Vec<String>,
    bsns_year: String,
    reprt_code: String,
}

impl FnlttMultiAcnt {
    const LOGICAL_OPERATION_ID: &'static str = "DS003-2019017";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl IntoIterator<Item = impl Into<String>>, bsns_year: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into_iter().map(Into::into).collect(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &[String] { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::FnlttMultiAcntJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttMultiAcnt_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttMultiAcnt.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_multi_acnt_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::FnlttMultiAcntXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttMultiAcnt_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttMultiAcnt.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_multi_acnt_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        if !(1..=100).contains(&self.corp_code.len()) { return Err(PrepareError::InvalidCardinality { operation: identity, parameter: "corp_code", minimum: 1, maximum: 100 }); }
        for value in &self.corp_code { require_nonempty(identity, "corp_code", value)?; }
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::CommaSeparated(&self.corp_code) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS003-2019019` (API ID `2019019`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS003&apiId=2019019>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct FnlttXbrl {
    rcept_no: String,
    reprt_code: String,
}

impl FnlttXbrl {
    const LOGICAL_OPERATION_ID: &'static str = "DS003-2019019";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(rcept_no: impl Into<String>, reprt_code: impl Into<String>) -> Self {
        Self {
            rcept_no: rcept_no.into(),
            reprt_code: reprt_code.into(),
        }
    }

    /// Returns the `rcept_no` input.
    #[must_use]
    pub fn rcept_no(&self) -> &str { &self.rcept_no }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Prepares the ZIP physical representation without performing I/O.
    pub fn prepare_zip(&self) -> Result<PreparedBinaryRequest, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttXbrl_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttXbrl.xml", identity, ZIP_OR_XML, Some("result"))?;
        Ok(PreparedBinaryRequest::new(parts))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "rcept_no", &self.rcept_no)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        let parameters = vec![
            QueryParameter { name: "rcept_no", value: QueryValue::Scalar(self.rcept_no.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS003-2019020` (API ID `2019020`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS003&apiId=2019020>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct FnlttSinglAcntAll {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
    fs_div: String,
}

impl FnlttSinglAcntAll {
    const LOGICAL_OPERATION_ID: &'static str = "DS003-2019020";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>, fs_div: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
            fs_div: fs_div.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Returns the `fs_div` input.
    #[must_use]
    pub fn fs_div(&self) -> &str { &self.fs_div }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::FnlttSinglAcntAllJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttSinglAcntAll_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttSinglAcntAll.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_singl_acnt_all_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::FnlttSinglAcntAllXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttSinglAcntAll_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttSinglAcntAll.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_singl_acnt_all_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        require_nonempty(identity, "fs_div", &self.fs_div)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
            QueryParameter { name: "fs_div", value: QueryValue::Scalar(self.fs_div.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS003-2020001` (API ID `2020001`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS003&apiId=2020001>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct XbrlTaxonomy {
    sj_div: String,
}

impl XbrlTaxonomy {
    const LOGICAL_OPERATION_ID: &'static str = "DS003-2020001";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(sj_div: impl Into<String>) -> Self {
        Self {
            sj_div: sj_div.into(),
        }
    }

    /// Returns the `sj_div` input.
    #[must_use]
    pub fn sj_div(&self) -> &str { &self.sj_div }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::XbrlTaxonomyJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_xbrlTaxonomy_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/xbrlTaxonomy.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_xbrl_taxonomy_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::XbrlTaxonomyXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_xbrlTaxonomy_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/xbrlTaxonomy.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_xbrl_taxonomy_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "sj_div", &self.sj_div)?;
        let parameters = vec![
            QueryParameter { name: "sj_div", value: QueryValue::Scalar(self.sj_div.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS003-2022001` (API ID `2022001`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS003&apiId=2022001>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct FnlttSinglIndx {
    corp_code: String,
    bsns_year: String,
    reprt_code: String,
    idx_cl_code: String,
}

impl FnlttSinglIndx {
    const LOGICAL_OPERATION_ID: &'static str = "DS003-2022001";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bsns_year: impl Into<String>, reprt_code: impl Into<String>, idx_cl_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
            idx_cl_code: idx_cl_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Returns the `idx_cl_code` input.
    #[must_use]
    pub fn idx_cl_code(&self) -> &str { &self.idx_cl_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::FnlttSinglIndxJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttSinglIndx_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttSinglIndx.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_singl_indx_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::FnlttSinglIndxXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttSinglIndx_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttSinglIndx.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_singl_indx_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        require_nonempty(identity, "idx_cl_code", &self.idx_cl_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
            QueryParameter { name: "idx_cl_code", value: QueryValue::Scalar(self.idx_cl_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS003-2022002` (API ID `2022002`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS003&apiId=2022002>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct FnlttCmpnyIndx {
    corp_code: Vec<String>,
    bsns_year: String,
    reprt_code: String,
    idx_cl_code: String,
}

impl FnlttCmpnyIndx {
    const LOGICAL_OPERATION_ID: &'static str = "DS003-2022002";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl IntoIterator<Item = impl Into<String>>, bsns_year: impl Into<String>, reprt_code: impl Into<String>, idx_cl_code: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into_iter().map(Into::into).collect(),
            bsns_year: bsns_year.into(),
            reprt_code: reprt_code.into(),
            idx_cl_code: idx_cl_code.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &[String] { &self.corp_code }

    /// Returns the `bsns_year` input.
    #[must_use]
    pub fn bsns_year(&self) -> &str { &self.bsns_year }

    /// Returns the `reprt_code` input.
    #[must_use]
    pub fn reprt_code(&self) -> &str { &self.reprt_code }

    /// Returns the `idx_cl_code` input.
    #[must_use]
    pub fn idx_cl_code(&self) -> &str { &self.idx_cl_code }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::FnlttCmpnyIndxJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttCmpnyIndx_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttCmpnyIndx.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_cmpny_indx_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::FnlttCmpnyIndxXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_fnlttCmpnyIndx_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/fnlttCmpnyIndx.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_fnltt_cmpny_indx_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        if !(1..=100).contains(&self.corp_code.len()) { return Err(PrepareError::InvalidCardinality { operation: identity, parameter: "corp_code", minimum: 1, maximum: 100 }); }
        for value in &self.corp_code { require_nonempty(identity, "corp_code", value)?; }
        require_nonempty(identity, "bsns_year", &self.bsns_year)?;
        require_nonempty(identity, "reprt_code", &self.reprt_code)?;
        require_nonempty(identity, "idx_cl_code", &self.idx_cl_code)?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::CommaSeparated(&self.corp_code) },
            QueryParameter { name: "bsns_year", value: QueryValue::Scalar(self.bsns_year.as_str()) },
            QueryParameter { name: "reprt_code", value: QueryValue::Scalar(self.reprt_code.as_str()) },
            QueryParameter { name: "idx_cl_code", value: QueryValue::Scalar(self.idx_cl_code.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

fn require_nonempty(operation: OperationIdentity, parameter: &'static str, value: &str) -> Result<(), PrepareError> {
    if value.is_empty() { return Err(PrepareError::MissingInput { operation, parameter }); }
    Ok(())
}

#[cfg(test)]
mod generated_request_tests {
    use super::*;
    use crate::{Authentication, RequestMethod};

    #[test]
    fn every_physical_request_matches_its_generated_vector() {
        {
            let input = FnlttSinglAcnt::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttSinglAcnt_json");
            assert_eq!(prepared.identity().logical(), "DS003-2019016");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttSinglAcnt.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttSinglAcnt::new("value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttSinglAcnt_xml");
            assert_eq!(prepared.identity().logical(), "DS003-2019016");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttSinglAcnt.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttMultiAcnt::new(["first,value", "회사 /+"], "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttMultiAcnt_json");
            assert_eq!(prepared.identity().logical(), "DS003-2019017");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttMultiAcnt.json");
            assert_eq!(prepared.encoded_query(), "corp_code=first%2Cvalue,%ED%9A%8C%EC%82%AC+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttMultiAcnt::new(["first,value", "회사 /+"], "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttMultiAcnt_xml");
            assert_eq!(prepared.identity().logical(), "DS003-2019017");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttMultiAcnt.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=first%2Cvalue,%ED%9A%8C%EC%82%AC+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttXbrl::new("value /+", "value /+");
            let prepared = input.prepare_zip().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttXbrl_xml");
            assert_eq!(prepared.identity().logical(), "DS003-2019019");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttXbrl.xml");
            assert_eq!(prepared.encoded_query(), "rcept_no=value+%2F%2B&reprt_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), ZIP_OR_XML);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttSinglAcntAll::new("value /+", "value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttSinglAcntAll_json");
            assert_eq!(prepared.identity().logical(), "DS003-2019020");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttSinglAcntAll.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B&fs_div=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttSinglAcntAll::new("value /+", "value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttSinglAcntAll_xml");
            assert_eq!(prepared.identity().logical(), "DS003-2019020");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttSinglAcntAll.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B&fs_div=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = XbrlTaxonomy::new("value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_xbrlTaxonomy_json");
            assert_eq!(prepared.identity().logical(), "DS003-2020001");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/xbrlTaxonomy.json");
            assert_eq!(prepared.encoded_query(), "sj_div=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = XbrlTaxonomy::new("value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_xbrlTaxonomy_xml");
            assert_eq!(prepared.identity().logical(), "DS003-2020001");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/xbrlTaxonomy.xml");
            assert_eq!(prepared.encoded_query(), "sj_div=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttSinglIndx::new("value /+", "value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttSinglIndx_json");
            assert_eq!(prepared.identity().logical(), "DS003-2022001");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttSinglIndx.json");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B&idx_cl_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttSinglIndx::new("value /+", "value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttSinglIndx_xml");
            assert_eq!(prepared.identity().logical(), "DS003-2022001");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttSinglIndx.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=value+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B&idx_cl_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttCmpnyIndx::new(["first,value", "회사 /+"], "value /+", "value /+", "value /+");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttCmpnyIndx_json");
            assert_eq!(prepared.identity().logical(), "DS003-2022002");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttCmpnyIndx.json");
            assert_eq!(prepared.encoded_query(), "corp_code=first%2Cvalue,%ED%9A%8C%EC%82%AC+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B&idx_cl_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = FnlttCmpnyIndx::new(["first,value", "회사 /+"], "value /+", "value /+", "value /+");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_fnlttCmpnyIndx_xml");
            assert_eq!(prepared.identity().logical(), "DS003-2022002");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/fnlttCmpnyIndx.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=first%2Cvalue,%ED%9A%8C%EC%82%AC+%2F%2B&bsns_year=value+%2F%2B&reprt_code=value+%2F%2B&idx_cl_code=value+%2F%2B");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
    }
}
