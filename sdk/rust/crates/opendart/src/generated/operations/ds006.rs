// @generated by opendart-tool; DO NOT EDIT.
// generator-schema: 3
// sdk-projection-sha256: 2fc49e8288624e5a8285a4adf5b54fe3fa03ab898926c339da1291d2f4a67eee

use crate::request::{QueryParameter, QueryValue, RequestParts};
use crate::{OperationIdentity, PrepareError, PreparedRequest, Representation};
use crate::validation::{require_format, require_length};
use super::super::responses::ds006 as response;
use super::super::{GENERATOR_SCHEMA, PROJECTION_CHECKSUM};

const JSON_ONLY: &[Representation] = &[Representation::Json];
const XML_ONLY: &[Representation] = &[Representation::Xml];

/// OpenDART logical operation `DS006-2020054` (API ID `2020054`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS006&apiId=2020054>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct EstkRs {
    corp_code: String,
    bgn_de: String,
    end_de: String,
}

impl EstkRs {
    const LOGICAL_OPERATION_ID: &'static str = "DS006-2020054";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bgn_de: impl Into<String>, end_de: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bgn_de: bgn_de.into(),
            end_de: end_de.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bgn_de` input.
    #[must_use]
    pub fn bgn_de(&self) -> &str { &self.bgn_de }

    /// Returns the `end_de` input.
    #[must_use]
    pub fn end_de(&self) -> &str { &self.end_de }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::EstkRsJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_estkRs_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/estkRs.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_estk_rs_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::EstkRsXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_estkRs_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/estkRs.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_estk_rs_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_length(identity, "corp_code", &self.corp_code, 8, 8)?;
        require_format(identity, "corp_code", &self.corp_code, "opendart-corp-code")?;
        require_nonempty(identity, "bgn_de", &self.bgn_de)?;
        require_length(identity, "bgn_de", &self.bgn_de, 8, 8)?;
        require_format(identity, "bgn_de", &self.bgn_de, "opendart-date")?;
        require_nonempty(identity, "end_de", &self.end_de)?;
        require_length(identity, "end_de", &self.end_de, 8, 8)?;
        require_format(identity, "end_de", &self.end_de, "opendart-date")?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bgn_de", value: QueryValue::Scalar(self.bgn_de.as_str()) },
            QueryParameter { name: "end_de", value: QueryValue::Scalar(self.end_de.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS006-2020055` (API ID `2020055`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS006&apiId=2020055>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct BdRs {
    corp_code: String,
    bgn_de: String,
    end_de: String,
}

impl BdRs {
    const LOGICAL_OPERATION_ID: &'static str = "DS006-2020055";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bgn_de: impl Into<String>, end_de: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bgn_de: bgn_de.into(),
            end_de: end_de.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bgn_de` input.
    #[must_use]
    pub fn bgn_de(&self) -> &str { &self.bgn_de }

    /// Returns the `end_de` input.
    #[must_use]
    pub fn end_de(&self) -> &str { &self.end_de }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::BdRsJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_bdRs_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/bdRs.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_bd_rs_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::BdRsXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_bdRs_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/bdRs.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_bd_rs_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_length(identity, "corp_code", &self.corp_code, 8, 8)?;
        require_format(identity, "corp_code", &self.corp_code, "opendart-corp-code")?;
        require_nonempty(identity, "bgn_de", &self.bgn_de)?;
        require_length(identity, "bgn_de", &self.bgn_de, 8, 8)?;
        require_format(identity, "bgn_de", &self.bgn_de, "opendart-date")?;
        require_nonempty(identity, "end_de", &self.end_de)?;
        require_length(identity, "end_de", &self.end_de, 8, 8)?;
        require_format(identity, "end_de", &self.end_de, "opendart-date")?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bgn_de", value: QueryValue::Scalar(self.bgn_de.as_str()) },
            QueryParameter { name: "end_de", value: QueryValue::Scalar(self.end_de.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS006-2020056` (API ID `2020056`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS006&apiId=2020056>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct StkdpRs {
    corp_code: String,
    bgn_de: String,
    end_de: String,
}

impl StkdpRs {
    const LOGICAL_OPERATION_ID: &'static str = "DS006-2020056";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bgn_de: impl Into<String>, end_de: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bgn_de: bgn_de.into(),
            end_de: end_de.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bgn_de` input.
    #[must_use]
    pub fn bgn_de(&self) -> &str { &self.bgn_de }

    /// Returns the `end_de` input.
    #[must_use]
    pub fn end_de(&self) -> &str { &self.end_de }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::StkdpRsJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_stkdpRs_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/stkdpRs.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_stkdp_rs_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::StkdpRsXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_stkdpRs_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/stkdpRs.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_stkdp_rs_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_length(identity, "corp_code", &self.corp_code, 8, 8)?;
        require_format(identity, "corp_code", &self.corp_code, "opendart-corp-code")?;
        require_nonempty(identity, "bgn_de", &self.bgn_de)?;
        require_length(identity, "bgn_de", &self.bgn_de, 8, 8)?;
        require_format(identity, "bgn_de", &self.bgn_de, "opendart-date")?;
        require_nonempty(identity, "end_de", &self.end_de)?;
        require_length(identity, "end_de", &self.end_de, 8, 8)?;
        require_format(identity, "end_de", &self.end_de, "opendart-date")?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bgn_de", value: QueryValue::Scalar(self.bgn_de.as_str()) },
            QueryParameter { name: "end_de", value: QueryValue::Scalar(self.end_de.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS006-2020057` (API ID `2020057`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS006&apiId=2020057>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct MgRs {
    corp_code: String,
    bgn_de: String,
    end_de: String,
}

impl MgRs {
    const LOGICAL_OPERATION_ID: &'static str = "DS006-2020057";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bgn_de: impl Into<String>, end_de: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bgn_de: bgn_de.into(),
            end_de: end_de.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bgn_de` input.
    #[must_use]
    pub fn bgn_de(&self) -> &str { &self.bgn_de }

    /// Returns the `end_de` input.
    #[must_use]
    pub fn end_de(&self) -> &str { &self.end_de }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::MgRsJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_mgRs_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/mgRs.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_mg_rs_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::MgRsXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_mgRs_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/mgRs.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_mg_rs_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_length(identity, "corp_code", &self.corp_code, 8, 8)?;
        require_format(identity, "corp_code", &self.corp_code, "opendart-corp-code")?;
        require_nonempty(identity, "bgn_de", &self.bgn_de)?;
        require_length(identity, "bgn_de", &self.bgn_de, 8, 8)?;
        require_format(identity, "bgn_de", &self.bgn_de, "opendart-date")?;
        require_nonempty(identity, "end_de", &self.end_de)?;
        require_length(identity, "end_de", &self.end_de, 8, 8)?;
        require_format(identity, "end_de", &self.end_de, "opendart-date")?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bgn_de", value: QueryValue::Scalar(self.bgn_de.as_str()) },
            QueryParameter { name: "end_de", value: QueryValue::Scalar(self.end_de.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS006-2020058` (API ID `2020058`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS006&apiId=2020058>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct ExtrRs {
    corp_code: String,
    bgn_de: String,
    end_de: String,
}

impl ExtrRs {
    const LOGICAL_OPERATION_ID: &'static str = "DS006-2020058";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bgn_de: impl Into<String>, end_de: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bgn_de: bgn_de.into(),
            end_de: end_de.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bgn_de` input.
    #[must_use]
    pub fn bgn_de(&self) -> &str { &self.bgn_de }

    /// Returns the `end_de` input.
    #[must_use]
    pub fn end_de(&self) -> &str { &self.end_de }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::ExtrRsJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_extrRs_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/extrRs.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_extr_rs_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::ExtrRsXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_extrRs_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/extrRs.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_extr_rs_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_length(identity, "corp_code", &self.corp_code, 8, 8)?;
        require_format(identity, "corp_code", &self.corp_code, "opendart-corp-code")?;
        require_nonempty(identity, "bgn_de", &self.bgn_de)?;
        require_length(identity, "bgn_de", &self.bgn_de, 8, 8)?;
        require_format(identity, "bgn_de", &self.bgn_de, "opendart-date")?;
        require_nonempty(identity, "end_de", &self.end_de)?;
        require_length(identity, "end_de", &self.end_de, 8, 8)?;
        require_format(identity, "end_de", &self.end_de, "opendart-date")?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bgn_de", value: QueryValue::Scalar(self.bgn_de.as_str()) },
            QueryParameter { name: "end_de", value: QueryValue::Scalar(self.end_de.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

/// OpenDART logical operation `DS006-2020059` (API ID `2020059`).
///
/// Source: <https://opendart.fss.or.kr/guide/detail.do?apiGrpCd=DS006&apiId=2020059>
#[derive(Clone, Debug, Eq, PartialEq)]
pub struct DvRs {
    corp_code: String,
    bgn_de: String,
    end_de: String,
}

impl DvRs {
    const LOGICAL_OPERATION_ID: &'static str = "DS006-2020059";

    /// Creates an operation input. Explicit contract validation occurs during preparation.
    #[must_use]
    pub fn new(corp_code: impl Into<String>, bgn_de: impl Into<String>, end_de: impl Into<String>) -> Self {
        Self {
            corp_code: corp_code.into(),
            bgn_de: bgn_de.into(),
            end_de: end_de.into(),
        }
    }

    /// Returns the `corp_code` input.
    #[must_use]
    pub fn corp_code(&self) -> &str { &self.corp_code }

    /// Returns the `bgn_de` input.
    #[must_use]
    pub fn bgn_de(&self) -> &str { &self.bgn_de }

    /// Returns the `end_de` input.
    #[must_use]
    pub fn end_de(&self) -> &str { &self.end_de }

    /// Prepares the JSON physical representation without performing I/O.
    pub fn prepare_json(&self) -> Result<PreparedRequest<response::DvRsJsonResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_dvRs_json", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/dvRs.json", identity, JSON_ONLY, None)?;
        Ok(PreparedRequest::new(parts, response::decode_dv_rs_json_response))
    }

    /// Prepares the XML physical representation without performing I/O.
    pub fn prepare_xml(&self) -> Result<PreparedRequest<response::DvRsXmlResponse>, PrepareError> {
        let identity = OperationIdentity::new("get_dvRs_xml", Self::LOGICAL_OPERATION_ID);
        let parts = self.prepare_parts("/api/dvRs.xml", identity, XML_ONLY, Some("result"))?;
        Ok(PreparedRequest::new(parts, response::decode_dv_rs_xml_response))
    }

    fn prepare_parts(&self, path: &'static str, identity: OperationIdentity, expected: &'static [Representation], expected_xml_root: Option<&'static str>) -> Result<RequestParts, PrepareError> {
        require_nonempty(identity, "corp_code", &self.corp_code)?;
        require_length(identity, "corp_code", &self.corp_code, 8, 8)?;
        require_format(identity, "corp_code", &self.corp_code, "opendart-corp-code")?;
        require_nonempty(identity, "bgn_de", &self.bgn_de)?;
        require_length(identity, "bgn_de", &self.bgn_de, 8, 8)?;
        require_format(identity, "bgn_de", &self.bgn_de, "opendart-date")?;
        require_nonempty(identity, "end_de", &self.end_de)?;
        require_length(identity, "end_de", &self.end_de, 8, 8)?;
        require_format(identity, "end_de", &self.end_de, "opendart-date")?;
        let parameters = vec![
            QueryParameter { name: "corp_code", value: QueryValue::Scalar(self.corp_code.as_str()) },
            QueryParameter { name: "bgn_de", value: QueryValue::Scalar(self.bgn_de.as_str()) },
            QueryParameter { name: "end_de", value: QueryValue::Scalar(self.end_de.as_str()) },
        ];
        Ok(RequestParts::new(path, identity, &parameters, expected, expected_xml_root, GENERATOR_SCHEMA, PROJECTION_CHECKSUM))
    }
}

fn require_nonempty(operation: OperationIdentity, parameter: &'static str, value: &str) -> Result<(), PrepareError> {
    if value.is_empty() { return Err(PrepareError::MissingInput { operation, parameter }); }
    Ok(())
}

#[cfg(test)]
mod generated_request_tests {
    use super::*;
    use crate::{Authentication, RequestMethod};

    #[test]
    fn every_physical_request_matches_its_generated_vector() {
        {
            let input = EstkRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_estkRs_json");
            assert_eq!(prepared.identity().logical(), "DS006-2020054");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/estkRs.json");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = EstkRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_estkRs_xml");
            assert_eq!(prepared.identity().logical(), "DS006-2020054");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/estkRs.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = BdRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_bdRs_json");
            assert_eq!(prepared.identity().logical(), "DS006-2020055");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/bdRs.json");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = BdRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_bdRs_xml");
            assert_eq!(prepared.identity().logical(), "DS006-2020055");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/bdRs.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = StkdpRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_stkdpRs_json");
            assert_eq!(prepared.identity().logical(), "DS006-2020056");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/stkdpRs.json");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = StkdpRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_stkdpRs_xml");
            assert_eq!(prepared.identity().logical(), "DS006-2020056");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/stkdpRs.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = MgRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_mgRs_json");
            assert_eq!(prepared.identity().logical(), "DS006-2020057");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/mgRs.json");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = MgRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_mgRs_xml");
            assert_eq!(prepared.identity().logical(), "DS006-2020057");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/mgRs.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = ExtrRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_extrRs_json");
            assert_eq!(prepared.identity().logical(), "DS006-2020058");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/extrRs.json");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = ExtrRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_extrRs_xml");
            assert_eq!(prepared.identity().logical(), "DS006-2020058");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/extrRs.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = DvRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_json().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_dvRs_json");
            assert_eq!(prepared.identity().logical(), "DS006-2020059");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/dvRs.json");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), JSON_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
        {
            let input = DvRs::new("00126380", "20240229", "20240229");
            let prepared = input.prepare_xml().expect("generated vector must prepare");
            assert_eq!(prepared.identity().physical(), "get_dvRs_xml");
            assert_eq!(prepared.identity().logical(), "DS006-2020059");
            assert_eq!(prepared.method(), RequestMethod::Get);
            assert_eq!(prepared.relative_path(), "/api/dvRs.xml");
            assert_eq!(prepared.encoded_query(), "corp_code=00126380&bgn_de=20240229&end_de=20240229");
            assert_eq!(prepared.authentication(), Authentication::ApiKeyQuery);
            assert!(!prepared.encoded_query().contains("crtfc_key"));
            assert_eq!(prepared.expected_representations(), XML_ONLY);
            assert_eq!(prepared.generator_schema(), GENERATOR_SCHEMA);
            assert_eq!(prepared.projection_identity(), PROJECTION_CHECKSUM);
        }
    }
}
